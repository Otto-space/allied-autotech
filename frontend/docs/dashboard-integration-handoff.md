# Dashboard integration handoff

Updated September 18, 2026. The full goal remains active; this document is not release approval. The controlling brief is `C:/Users/PC/.codex/attachments/65c2e449-ba40-49b8-a627-caf6bb76d730/goal-objective.md`, with the original 18-section brief it references and the user's later answers below.

## Approved decisions

- **Quicksand throughout**, explicitly reconfirmed by the user on September 17. This supersedes the detailed brief's Nibble display-heading instruction. The unused Nibble file remains on disk; it is no longer loaded.
- Selected owner: **alliedautotechltd@gmail.com**. The configured local development database has no matching account and no owner, as verified with the read-only provisioning inspector. Do not auto-create or auto-verify this account. The approved public business contact remains alliedautotech26@gmail.com.
- STAFF: branch bookings, inventory, fulfilment, vehicles, inspections and support. Finance and privileged account management are denied; own-account security is allowed. Payment/invoice endpoints and services now deny STAFF; operational price/payment projections and related vehicle/service actions still need a separate scope audit.
- Reference: `C:/Users/PC/Downloads/superadmin-admin dashboard`, an extensionless WebP, native 1864 × 1398. Inspected using `view_image`. Exclude browser chrome and the outer grey presentation background. No Figma file was supplied or inspected.
- No ownership-transfer policy is approved. Refunds, delivery fees, reservation terms and other unresolved business policies in the backend owner checklist remain unchanged. No production deployment, real messages or payment-provider actions were performed.

## Implementation map

| Requirement                 | Evidence and status                                                                                                                                                                                                                                                                                                                                   | Remaining work                                                                                                                                                                                                                                                             |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Inspect before modifying | Frontend instructions, installed Next documentation, manifests, role middleware, organization services/policies, auth, schema/migrations, moderation, tests and owner checklist inspected.                                                                                                                                                            | Continue tracing every affected consumer before changing contracts.                                                                                                                                                                                                        |
| 2. Existing architecture    | Shared `dashboard-shell.tsx`, session context, API client, domain routers and transaction patterns reused.                                                                                                                                                                                                                                            | Aggregates and team workflows are integrated into these shared boundaries; no parallel auth or role models.                                                                                                                                                                |
| 3. Reference and branding   | Reference inspected. Dark sidebar, white search/account header, Quicksand and approved palette integrated. Shared role-scoped overview now renders five cards, date controls, activity/status panels and recent records.                                                                                                                              | Overview passed 12 fixture and four real-API browser cases; current sidebar expansion/collapse verification is tracked below.                                                                                                                                              |
| 4. Grouped navigation       | New `lib/dashboard-navigation.ts` and `app/components/dashboard-navigation.tsx`: 7 customer, 6 STAFF, 8 administrator groups; child permissions, current-group expansion, collapsed inactive groups, native keyboard disclosures, permitted-page search and mobile menu. Deep links preserved.                                                        | Earlier 13 role/navigation cases passed with the aggregate overview. The latest icon rail, scrollbar and boundary tests are pending the new production build.                                                                                                              |
| 5. Role boundaries          | Existing four roles, session/MFA/ownership checks retained. New navigation excludes STAFF finance/team links and searches only permitted destinations.                                                                                                                                                                                                | Payment/invoice routers and service policies now enforce administrator-only finance; direct frontend links do not mount finance children for STAFF. Audit remaining operational financial projections and branch/customer scope. Navigation does not enforce API security. |
| 6. Sole owner               | Forward migration `20260917190000_protect_sole_owner` adds singleton partial index, active/verified check and update/delete protection. Operator script promotes only the exact selected existing active verified account, checks installed protection, locks provisioners/candidate, revokes sessions and audits. Dedicated disposable tests passed. | Provision the actual selected account only when it exists and is verified in the intended environment. No real account was changed.                                                                                                                                        |
| 6. Team and invitations     | Implemented exact-email verified-account search/promotion, authenticated bound STAFF-to-ADMIN invitations, safe status/cursor lists and revocation. Current-password proof, MFA/CSRF, inviter recheck, session revocation, audits and transactional access-change locking are enforced. Generic role edits only demote ADMIN to STAFF.                | Focused API/policy, 17 browser cases and the final 12-case team regression passed. Real email delivery remains untested. The forward migration revokes unused legacy links and has not been applied to the real environment.                                               |
| 7. Analytics                | Two scoped aggregate endpoints provide Lagos-date counts, activity, statuses, previews and administrator-only exact financial totals. The shared SVG chart needs no added package.                                                                                                                                                                    | Aggregate API and bounded refresh are implemented. Complete the final browser and source-parity evidence below; no unmeasured conversion or growth figures are shown.                                                                                                      |
| 8. Reviews                  | Existing owned eligibility, pending moderation, administrator approval/rejection and public approved-only projection retained. New homepage preview reuses public SSR seeds and ReviewCard; failed refresh hides stale reviews, with empty/error/retry states. No anonymous solicitation.                                                             | Group moderation under support (navigation implemented); verify cache/moderation behavior in broader final role matrix. No customer edit-review endpoint should be added without re-moderation.                                                                            |
| 9. Domain correctness       | Existing payment/idempotency/stock/reservation invariants preserved. Coverage inventory now includes 231 operations; homepage review consumer added. Backend full disposable regression suite passed with shared singleton-owner fixtures.                                                                                                            | Resolve previously documented financial correctness gaps before release. Keep unsafe manual bulk expiry disabled.                                                                                                                                                          |
| 10. Business decisions      | Owner identity, reference and STAFF scope answered; typography subsequently confirmed.                                                                                                                                                                                                                                                                | Continue independently while genuine remaining business/configuration decisions are outstanding.                                                                                                                                                                           |
| 11. Verification            | See bounded results below.                                                                                                                                                                                                                                                                                                                            | Complete current team regression/visual evidence, remaining operational-finance scope audit, date analytics and full reference layout.                                                                                                                                     |
| 12. Continuation            | This handoff records current source, decisions and exact unfinished stages.                                                                                                                                                                                                                                                                           | Keep the goal active until the complete objective is delivered and verified.                                                                                                                                                                                               |

## Target permission matrix

This is the approved target, not a claim that every existing backend endpoint already enforces it.

| Capability                           | CUSTOMER              | STAFF                           | ADMIN                          | SUPER_ADMIN                             |
| ------------------------------------ | --------------------- | ------------------------------- | ------------------------------ | --------------------------------------- |
| Customer records/reviews             | Own eligible records  | No impersonation                | No impersonation               | No impersonation                        |
| Operational records                  | Own permitted records | Approved branch/assignment only | Explicit business operations   | Administrator operations                |
| Financial records/actions            | Own permitted records | Denied                          | Permitted, existing invariants | Same invariants, no bypass              |
| Promote verified CUSTOMER to STAFF   | No                    | No                              | Controlled workflow            | Controlled workflow                     |
| Invite existing STAFF to ADMIN       | No                    | No                              | Protected invitation           | Protected invitation                    |
| Generic ADMIN/SUPER_ADMIN assignment | No                    | No                              | No                             | No                                      |
| Initial owner provisioning           | No                    | No                              | No                             | Operator-only selected verified account |
| Alter/delete/deactivate owner        | No                    | No                              | No                             | No ordinary operation                   |
| Own account security                 | Yes                   | Yes                             | Yes                            | Yes                                     |
| Review moderation/global analytics   | No                    | No                              | Authorized queries             | Authorized queries                      |

## Completed security and support increment

- `support-create-form.tsx` clears anonymous drafts/proposals on account changes, guards delayed validation and late responses, aborts pending requests and retains uncertain-submission locks.
- `public-enquiry-form.tsx` deduplicates those locks; `service-detail.tsx` retains quotation uncertainty across public-data reload/remount.
- `support-widget.tsx` clears help queries on account changes. Narrow/short viewports use a bottom support bar with reserved space and focus scrolling. Physical-device software-keyboard behavior is still unverified; browser viewport geometry is tested.
- White inverse branding fixes red-on-ink contrast in the footer and sidebar. Brand action red remains unchanged.
- `home-reviews-preview.tsx` displays up to three approved public reviews without fabricated identity, figures or unsolicited review dialogs.
- `backend/docs/initial-owner-provisioning.md` documents read-only inspection, explicit promotion and database guards. Existing applied migrations were not edited. Disposable test fixtures share one synthetic protected owner without disabling protections.

## Actual verification

- Backend: 31 forward migrations replayed on fresh disposable databases. Initial full database-enabled suite after owner changes: **169 passed**, 45 files; the separate owner suite is intentionally skipped in that run because it requires its own fresh database.
- Dedicated owner suite: **3 passed** after the final installed-protection preflight, including invalid/unverified/inactive account selections, competing provisioners, direct database bypass attempts, credential preservation, session revocation and audit. Final database: `allied_owner_final_20260917_test`.
- Backend typecheck, script typecheck, lint, full formatting and production TypeScript build passed after the owner changes.
- Frontend: 106 unit tests passed again after shell regrouping and Quicksand confirmation. Production builds, lint and typechecks passed for the public changes and initial shell. Final Quicksand/search production build passed.
- Browser public increment: **123 passed**, then three support-accessibility failures from the old sidebar red branding stopped the run; 11 new contact-reset cases had not run. The 123 passes include all 27 public-action/privacy cases, 7 public-page cases and 57 SEO/review cases. Footer contrast and support-bar geometry passed from 320 to 1280 and landscape. This is not a claim of 137/137 success.
- New shell tests initially rejected malformed test envelopes (missing required message), then exposed a real search Escape/retyping defect. Both were fixed. All ten role/navigation/responsive cases passed on the final build; the complete shell/public/support run then passed **147/147** in 5.7 minutes.
- Playwright uses Edge because Browser/IAB tooling was unavailable in the preceding setup. Test media and records are isolated fixtures. No synthetic records were added to a real environment.

## Reference extraction and fidelity ledger

Reference app region is approximately 1655 × 1133 after excluding framing. Sidebar is about 300px, top header about 80px, content gutters about 36px. Summary cards have soft tints and rounded corners; two subsequent rows split about 2:1.

| Comparison point     | Current evidence / intentional adaptation                                                                                                                                                                                                                                                                                                                            |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Branding/copy        | Allied name and supported automotive destinations replace reference ecommerce brand, products and fabricated activity.                                                                                                                                                                                                                                               |
| Typography           | User-approved Quicksand throughout supersedes Nibble.                                                                                                                                                                                                                                                                                                                |
| Palette              | True white header/content, ink sidebar, restrained red active accent, existing semantic status colours.                                                                                                                                                                                                                                                              |
| Navigation           | Compact grouped sidebar and per-child permissions; inactive groups collapse and current group expands. Reference's flat demo destinations are intentionally replaced.                                                                                                                                                                                                |
| Header               | Permitted-page search, own notifications/account and logout work through existing routes. No language/theme/fullscreen controls without a supported purpose.                                                                                                                                                                                                         |
| Overview composition | Still incomplete: five aggregate cards, analytics/list and table/list remain to implement after backend contracts. No fidelity sign-off.                                                                                                                                                                                                                             |
| Responsive behavior  | Mobile navigation is a normal expandable region with Escape/focus return, not a modal. Checks passed at 360/390/768/1024/1440 and the approximately native app region 1655 x 1133. Reference, 1655px and 390px screenshots were inspected with view_image. Sidebar is currently 264px (reference about 300px); reconcile its width with the completed overview grid. |

## Earlier continuation (historical; latest verification below supersedes it)

1. Public/support/shell verification passed 147/147; financial page verification passed 27 distinct cases across a 26-pass run and one corrected-fixture rerun. One optional storage-allowlist evidence case remains skipped. Final backend checks and frontend lint/type/format passed. All 302 runtime/configuration/public/packaging files were compared; the sole type-only declaration difference was synchronized.
2. Payment/invoice finance denial and embedded invoice/deposit-payment omission are implemented and verified. Continue the scope audit for private commercial pricing and payment-related actions embedded in vehicle/service/order routes; commercial order totals, quote lines and deposit-policy snapshots currently remain operational context. Preserve approved branch work and customer-owned finance.
3. The verified-account team lifecycle is implemented; finish the in-flight regression and visual checks recorded in the September 18 increment below, then proceed to remaining scope and analytics work.
4. Add role-scoped aggregate contracts and reference overview composition, then the complete security and visual regression matrix.

The user development server on port 3000 remains untouched. Production checks use the external snapshot `C:/Users/PC/AppData/Local/Temp/allied-audit-build-c625ba14b7ba4854a2a0112cba8b75b2` on port 3001; isolated SSR test fixtures use 5011. Do not restart a live test merely because a wait yielded; resume its returned session handle.

## Finance boundary verification

- `/staff/payments` and `/staff/invoices` retain their URLs but require ADMIN/SUPER_ADMIN with verified MFA in both middleware and domain policies. All payment evidence/refund and invoice read/create/issue/void actions in these routers are covered. Existing CSRF, version, transaction and separation-of-duties controls remain. Customer-owned routes are unchanged.
- `FinanceAccess` prevents financial children from mounting on STAFF payment/invoice direct links. Three browser cases confirm zero finance requests. Administrator invoice creation/issue/void, payment review, exact-kobo refunds, uncertainty/idempotency and different-operator decisions remain tested.
- Five disposable real-API finance cases passed, including all roles, MFA denial and a role change checked on an existing session. Commerce/payment focused regression passed. The subsequent full backend run passed 173 tests and failed only a repeated singleton-owner MFA fixture count; its named synthetic factors are now reset within the disposable suite, and all 17 affected MFA tests passed again. This was a fixture-isolation defect, not a weakened authentication assertion. The separately gated owner suite had already passed three scenarios on a fresh database.
- OpenAPI required-role annotations and descriptions, authorization matrix, generated client types and 225-operation coverage were updated. Role annotation generation now classifies financial `/staff` URLs as administrator-only. General invitation annotations/behavior still need the planned secure lifecycle rewrite.

## Operational finance projection increment — 17 September, evening

- `orders.repository.ts` now chooses whether to select the invoice relation for operational list/detail/status responses. STAFF never receives that relation; administrators and customers retain authorized invoice views. Server-owned order values and clearance dates remain available for fulfilment.
- `service-operations.repository.ts` similarly excludes `depositPayment` from STAFF list/detail/assignment/status/disruption responses. Internal transaction checks still use authoritative payment state. Booking details and mutations now reject inactive staff branches, matching existing list authorization.
- `operationalOrderSchema` and the staff booking schema consume those narrower projections without making invoice/payment fields optional in the customer contracts. No new endpoint, dependency or database migration was required.
- Disposable API coverage: seven finance-boundary tests passed, including two new operational cases; existing commerce, deposit-slot and service-flow regressions passed. The final service/deposit rerun passed three cases, including STAFF status/disruption responses with no deposit record. One initial synthetic order fixture omitted the required confirmation timestamp; it was corrected without changing production constraints.
- Production browser coverage: seven distinct cases passed across a six-pass run and one rerun. The rerun updates an older mobile navigation assumption to explicitly open and close the collapsed menu before reviewing a no-show. The new order case navigates list/detail, confirms a versioned fulfilment change, and performs no finance requests. Existing quotation revisions, work-order milestones and uncertain-result recovery passed.
- Production frontend build and standalone packaging passed. Backend types, lint, full formatting and build passed; frontend types and lint passed after an unused test-fixture binding was removed. OpenAPI validation passed for 188 paths and the inventory remains 225 operations. Frontend full formatting passed after normalizing the regenerated inventory. All four changed runtime files matched the verified production source snapshot; generated type-only declarations were synchronized afterward.
- All owned runtime servers were stopped after browser verification. The user server on port 3000 was untouched. No real account, outgoing message, payment, deployment or owner provisioning was performed.

The September 18 increment below supersedes the former invitation implementation and its security gaps.

## Team & Access increment ? September 18

- Reused organization routes, Prisma models, session/MFA/CSRF, password verification, encrypted outbox, audit service, shared dashboard and mutation-review components. New `team-access.service.ts` and `team-access-security.ts` implement existing verified-customer promotion and bound administrator invitations. No parallel role/authentication model or dependency was introduced.
- The forward `20260917220000_bind_admin_invitations` migration adds recipient binding, pending uniqueness and immutable binding/terminal-state guards. It revokes unused legacy links while preserving history. All 32 migrations replayed on a fresh disposable `allied_team_20260917_test`; no real database migration or account change was performed.
- Both ADMIN and SUPER_ADMIN can invite existing verified active STAFF; only the intended MFA-assured recipient can explicitly accept with current-password proof and CSRF. Acceptance rechecks inviter authority, account/email/branch/session state, consumes once, retains credentials and revokes sessions. Global access-change locking serializes acceptance/revocation/access changes. Generic role edits allow only owner-controlled ADMIN-to-STAFF demotion.
- Team UI includes exact-email account search, branch promotion, reviewed invitations, own/all invitation status and cursor pages, reviewed revocation and audit-history link. Acceptance strips the fragment, sends no referrer, clears credentials/session data across tabs and rejects late responses. Queued email is never labelled delivered. Unknown mutation outcomes stay locked for reconciliation.
- Focused real-API/policy verification passed 15 tests across four files. The first invocation mistakenly used RUN_DB_TESTS and skipped seven database tests; it was corrected to RUN_DATABASE_TESTS=true before the 15-pass run. Additional recipient-assurance, suspension/revocation and acceptance-versus-revocation tests were subsequently added and are in the full disposable suite currently running.
- Production build and packaging passed with the final cross-tab invalidation fix. The initial 17-case Team browser suite passed. A further owner-demotion browser case passed. Added visual-evidence runs initially failed only an incorrect expected title; the assertion now uses the actual route title, and both visual cases are rerunning. No runtime source changed after this build.
- OpenAPI validation passed for 191 paths, inventory regenerated for 229 operations and generated API types updated. Backend types/lint passed and production TypeScript build is running. Frontend typecheck passed before the final test-only additions; final lint/type/unit chain is running.
- The full goal remains incomplete: remaining operational financial projections/actions, role-scoped date analytics/reference overview, broader security/visual regressions and genuine release configuration/business blockers still require work. Do not treat this stage as release approval.

- Full disposable backend regression completed: **187 passed, one failed, three skipped** across 48 files. The sole failure was the old hardcoded 225-operation handbook count; after updating it to 229, the affected handbook test passed. All 12 team integration cases passed within the full run, including the new assurance and revoke-versus-accept races. The three skipped tests are the separately gated owner-provisioning suite, which previously passed on its own fresh database.
- Backend final typecheck, lint, 191-path OpenAPI validation and production TypeScript build passed. Full formatting check is running.
- Browser verification now covers **18 distinct passing team scenarios**: the 17-case main run plus owner demotion. Both added visual cases passed after correcting the title assertion. Mobile promotion/invitation dialogs passed axe and overflow checks; screenshot review found two active sidebar children on the nested invitation route. Navigation now chooses only the longest matching destination, with a browser assertion added. This small runtime correction is rebuilding before the final team/navigation regression; do not claim its browser pass yet.

- Frontend lint and typecheck passed. The default unit run passed 94 tests but failed to start two workers (public-resource/account-mutation) during concurrent build activity; this is not a complete unit-suite pass. Rerun with one worker after the final production build, keeping the worker-start errors recorded. Backend full formatting passed, and the corrected handbook-count test passed separately.

### Next overview design (inspection only; no aggregate code written yet)

- Existing overview components are `admin-overview.tsx` (administrator queue/reconciliation status; STAFF summary only) and `dashboard-overview.tsx` (five-booking first page and three links). No general aggregate module/endpoint exists. Keep existing operational status available as a contextual panel or link.
- Reuse `/customers` and `/staff` route authorization, existing Prisma entities, session/resource hook and common success envelopes. Prefer two role-bound overview reads with strict Lagos calendar-date inputs and bounded range; derive customer/active staff branch from the actor. Never accept a customer/branch scope override for ordinary users.
- Bookings/orders/quotes/inspections support real creation-date counts. Customer quote counts must exclude DRAFT. Payment has `amountKobo`, `status`, `succeededAt`, `settledAttemptId`; refunds have `amountKobo`, `status`, `processedAt`. Financial widgets must be admin-only with per-currency exact sums and explicit settled-request/refund definitions, not unpaid order value called revenue. STAFF must not query finance tables.
- Suggested overview composition remains five softly tinted cards, daily booking/order activity chart plus compact status list, recent bookings table plus recent orders list. Label every date basis and current-status breakdown, show actual zeros only on successful reads, and retain accessible chart summaries and working drill-down links. No invented conversion/comparison statistics or new chart dependency required.

- Remaining STAFF finance scope confirmed in source: `vehicles.repository.ts` includes acquisitionCostKobo/acquisitionCurrency in `staffVehicleSelect`; list/detail/create/update responses share it, and `VehiclesService.createVehicle/updateVehicle` currently allow STAFF to submit those fields. Frontend `staff-vehicle-schemas.ts`, `staff-vehicle-detail.tsx`, `vehicle-record-form.tsx` and `lib/forms/vehicle-record.ts` assume acquisition fields. Fix query/response projections and reject STAFF financial-field writes together, preserving branch operational vehicle edits and admin acquisition access. Vehicle-sale response projections inspected do not include payment relations, but related consequential routes still need scope review.
- Source parity audit after the navigation correction compared 305 runtime/configuration/public/packaging files against the external build snapshot: zero differences. Final build and one-worker unit rerun remain active.

### Final bounded verification for this increment

- Final production build/standalone packaging passed after the active-navigation correction. The one-worker frontend unit rerun passed **106/106 in 13 files**, resolving the two worker-start timeouts without changing tests or constraints.
- The final combined Team & Access/navigation browser run passed **31/31 in 2.0 minutes** on this build (terminal exit 0). It includes all four roles, staff direct-finance denial, 360/390/768/1024/1440/1655 navigation widths, promotion/invitation/revoke/accept, owner demotion, unknown outcomes and cross-tab late-response isolation. Production fixtures ran on 127.0.0.1:3001 with Playwright/Edge because the Browser plugin is unavailable.
- Latest 1440px team screenshot and 390px promotion dialog were inspected. The final sidebar marks only Promotions & invitations active; header, fields and dialog controls are readable without overlap. Synthetic fixtures exist only in isolated tests. Overview reference fidelity is still incomplete.
- Backend formatting and corrected handbook test passed; final frontend formatting/focused lint are being collected. No real account, message, payment, owner provisioning, deployment, push or commit occurred.

- The owned production server on port 3001 was stopped after verification. The user server on port 3000 was untouched. No background test or build remains except the final frontend formatting/lint gate until its terminal result is collected.

- Final focused frontend lint passed. Full formatting found only newly appended validation-progress.md; normalize that file and the final handoff, then check the touched documents. All other files passed the full formatting scan. All owned runtime servers/builds/tests are stopped.

- Final touched-document formatting passed after normalization (exit 0). No checks remain running.

## Vehicle acquisition-cost boundary ? September 18, subsequent increment

- Previous goal turn classified as progress: secure Team & Access plus verified production browser and API changes. Goal remains active and full scope unchanged.
- `vehicles.repository.ts` now selects acquisitionCostKobo/acquisitionCurrency only when explicitly authorized; internal/STAFF queries default to exclusion. List/detail/create/edit service responses pass the admin/owner flag. STAFF cost writes, including null, are rejected before mutation. Other vehicle specifications and acquiredAt remain operational; staff edits preserve stored cost.
- Frontend cost fields are optional only for operational projections; `parseAdminVehicle` still requires the full administrator projection. Detail and record forms use current role to render/submit acquisition controls. Administrator exact-kobo handling is retained.
- Added one real-API finance case covering list/detail/create/edit, null/value tampering, preserved storage, admin/owner and customer/unassigned-staff boundaries. Initial four-file run passed 27 cases and failed only an older vehicle test that still had STAFF setting cost. That fixture now seeds internal cost separately and checks STAFF response omission. Final finance+vehicle-flow rerun passed 9 cases; together the four affected files have 28 passing distinct cases. Backend types/lint and OpenAPI export passed; no new migration.
- Expanded existing vehicle-record browser scenario for STAFF and ADMIN, with field/payload omission for STAFF and exact-kobo administrator preservation. Browser run pending production build.
- Shared `site-header.tsx` changed externally during this work: a malformed `{setOpen.Sign in}` label was minimally repaired to Sign in. An unused SessionState import was removed, then concurrent edits introduced a new session state and restored the import. Do not overwrite these ongoing changes. An asynchronous coordination question asks whether the user is editing the header; leave it alone unless they request completion. Frontend check saw transient missing SessionState while edits overlapped; global checks must be revalidated against current state. Current vehicle changes are separate.
- Owned live handles at this point: production build **19270** (external snapshot), no browser server running. Earlier 50200 tests and 15337 OpenAPI/lint are terminal success; 21962 frontend chain terminated on the transient header type error. Poll the build handle; never restart a live build merely because observation yielded.

### User steering: public header account destination

The user clarified that their live header edits were intended to replace Sign in with Dashboard after login and route by the returned role. This authorizes completing the header, superseding the earlier leave-header assumption.

- `site-header.tsx` now reads the existing session contract through the shared API client, validates known roles and session expiry, and presents CUSTOMER ? /dashboard and STAFF/ADMIN/SUPER_ADMIN ? /admin in both desktop/mobile menus. Existing MFA and dashboard route guards remain authoritative. Account changes abort obsolete reads, clear the old link and revalidate; visibility refresh is coalesced/limited to 30 seconds and online refresh is supported. No browser credential storage or role model was added.
- A narrowly scoped `optionalSession` client option suppresses anonymous invalidation only for GET /auth/session. Protected requests and other methods still invalidate normally. If a previously verified header session receives 401, it clears shared account state. This prevents initial anonymous probes from clearing public drafts or entering a refresh loop.
- Added four API-client boundary unit cases and 14 browser cases for all roles at 390/1440, cross-tab role/logout changes, anonymous-draft preservation, late replies and MFA. These checks are not yet complete.
- Vehicle-only production build **19270** passed; it predates the new header and cannot verify it. Current frontend type/lint/unit handle **70315** is running. Need synchronize/rebuild latest header/client source, then run vehicle-stock and header-session browser suites plus affected public/session regressions. No owned runtime server is running yet.

### Verified header routing and quotation privacy — September 18

- The header now shows Dashboard for authenticated accounts: CUSTOMER goes to `/dashboard`; STAFF, ADMIN and SUPER_ADMIN go to `/admin`. Anonymous users retain Sign in. All 14 production browser cases passed for desktop/mobile roles, cross-tab role changes/logout, anonymous draft preservation, obsolete responses and the existing MFA gate. Desktop 1440px and mobile 390px screenshots were inspected. Quicksand remains throughout.
- Frontend types and focused header/client/test lint passed, as did all 110 unit tests in 13 files with one worker. Production build and standalone packaging passed on the synchronized external snapshot. This build includes the header, optional anonymous session probe and vehicle acquisition controls.
- Backend customer booking queries now exclude never-issued quotations, and the common customer serializer also protects mutation/replay responses. Both DRAFT records and VOID records replaced before issue remain internal; issued/accepted history and authorized staff draft access remain available. All ten customer rereads use the customer projection. Locked transactional reads keep their internal operational data until the customer response is serialized.
- Real disposable API checks passed 9 finance/service cases plus 2 booking-deposit cases. The booking test was then extended with a draft before rescheduling; its final 2-case rerun passed, including original and replay response omission. Backend types, lint, production build, full formatting and 191-path OpenAPI validation passed. No migration or real account change was needed.
- Coverage inventory remains 229 operations and now documents customer quotation privacy. An initial formatting command named a nonexistent inventory Markdown file; the correct generated files are `api-coverage.md` and `api-coverage.json`.
- Current owned runtime: production server handle **69402** at 127.0.0.1:3001. Browser batch **20429** is still running; all 14 header cases passed, support and vehicle results are pending. The public-account group was skipped because RUN_PUBLIC_ACTION_TESTS was absent; rerun it with that flag after this batch, using its isolated 5011 fixture server. Do not count skipped cases as verified.
- Next: collect current browser results, fix any failures, run enabled public-account regressions and inspect vehicle screenshots. Stop only owned runtime afterward. The full goal still requires role-scoped aggregate analytics and the five-card/chart/table reference overview, remaining scope/action review and the documented release blockers. No deployment, commit, push, message, payment or real owner provisioning occurred.

### Browser regression follow-up

- The initial combined auth/header/support/vehicle batch completed with **72 passed and 30 skipped**. Of those skips, 27 belong to the explicitly gated public-account suite and three to configured-storage upload scenarios. Both new vehicle role cases passed, including 320px axe/overflow, exact values and no runtime errors. Their screenshots were inspected; they show readable vehicle inputs without overlap.
- The enabled public-account/public-page batch completed **27 passed and 7 failed**. All seven failures were console-health assertions caused by the fixture returning 404 for the new `/auth/session` probe (six requests across three pages and their account-change refreshes). The existing focus, reset, overflow and axe assertions passed. The fixture now supplies a valid synthetic customer session; console assertions remain unchanged. The entire 27-case public-account suite is rerunning as handle **42830**. The seven public-page tests already passed at 320/768/1024/1440 and for search/retry behavior.
- Source parity compared **304** app/lib/public/script/configuration files against the verified production snapshot with **zero differences**, after syncing the formatting-only generated declaration change. The full frontend formatter initially flagged that generated declaration; it was normalized, and full format check is running as **92131**. Fixture-focused lint is **83893** until its terminal result is collected.
- Production server **69402** remains owned and running on 3001. No backend tests/builds remain running. The user server on 3000 remains untouched.

### Final results for header / vehicle-cost / quotation privacy

- The corrected public-account rerun has reported all **27 cases passing**; collect terminal runner exit before closing it. All seven former console-health failures passed unchanged once the fixture implemented the legitimate session read. The seven separate public-page checks also passed. Combined with the first 72-pass batch, this is **106 distinct passing browser cases**, with three configured-storage vehicle-upload cases still skipped.
- Final full frontend formatting and fixture-focused lint passed (terminal exit 0). Backend production build, lint, types, formatting and OpenAPI validation are complete. All 110 frontend unit tests passed. No current runtime source has changed since the verified production build.
- Full goal remains active. Next implementation stage is the role-scoped aggregate API and reference overview (five cards, real daily activity, status list, recent records), following the plan above; complete the remaining operational action-scope audit and release blockers. Owner provisioning remains unavailable until the selected email belongs to an existing verified account. Do not fabricate or auto-verify it.

- Final public-account runner **42830** exited 0: 27/27 passed in 2.5 minutes. The 106-case distinct browser total is confirmed. Temporary production server shutdown is being collected; user port 3000 remains untouched.

- Owned production server **69402** was stopped after verification. All owned test/build/format processes are terminal; no new server is left running.

## Role-scoped aggregate overview — September 18 implementation

- Previous goal turn made verified progress: role-aware header, vehicle acquisition-cost boundaries, customer quotation privacy and 106 distinct passing browser cases. The full goal remains active.
- Added `backend/src/modules/overview`: strict customer/staff GET endpoints with actor-derived customer/active-branch/organization scope, 1–90 inclusive Lagos dates, RepeatableRead aggregate snapshot, daily bookings/orders, all-scope counts, current booking statuses and bounded privacy-safe recent records. Only administrators query/receive exact settled-payment and processed-refund sums. Definition details and official transaction/timezone references are in `overview-metrics.md`.
- Added forward migration `20260918092000_overview_date_indexes` with nine date/scope indexes. All 33 migrations replayed on fresh disposable **allied_overview_20260918_test**; Prisma validation passed and migration diff reports no difference. No real database was migrated.
- Five new API tests now pass, including customer/branch isolation, stale branch denial, MFA/role/scope tampering, full counts beyond preview limits, Lagos boundary/leap dates, unpublished quote exclusion, zero-day filling, and payment/refund sums above Number.MAX_SAFE_INTEGER. Initial fixture failures were corrected: session creation must precede its MFA timestamp, validation uses the existing 422 status, and new orders require a branch. No production invariant was relaxed. Subsequent fresh overview+finance+service+OpenAPI regression passed **15/15**.
- Shared `RoleOverview` replaces the customer summary and admin overview entry. Five tinted cards, daily SVG activity with clickable legend/day slider/data table, booking-progress list, recent booking table/order list, date presets/custom range, honest loading/empty/error states and metric definitions. Existing admin operations remain lazy-mounted behind an expandable panel. STAFF has no finance widget or operations request. `useOverview` wraps the existing resource/client and adds visible/online minute refresh with error backoff.
- New `overview.css` follows the reference grid, white surfaces, rounded panels, five pastel cards, and 300px sidebar at the 1655px reference app width. User-approved Quicksand retained; no AI image generation. Reference image was inspected with view_image. Fidelity comparison is still pending actual rendered screenshots.
- Frontend types and focused runtime/component lint passed after replacing BigInt literals with the existing ES target-compatible BigInt constructor. Fixture/navigation/cart test types and lint also passed. Added overview browser cases for all roles, 320/360/390/768/1024/1655 widths, dates/errors/empty states, chart interactions and late-response isolation, plus three date/exact-money/parser unit cases. These new browser/unit cases are not yet run.
- OpenAPI validates **193 paths**, inventory **231 operations**; generated types/coverage refresh is **84803**, currently formatting. Backend validation/diff/build/format chain **51823** is live (validation and no-drift already passed). Frontend production snapshot build **42432** remains live; poll it, never restart solely for an observation timeout. New unit/docs formatter **36022** completed. No owned runtime server is running yet; user port3000 untouched.
- Next: collect build/check handles, run new unit tests, start the snapshot at3001 and run `overview.spec.ts`, `dashboard-navigation.spec.ts`, `cart-maintenance.spec.ts`; inspect 1655/1440/mobile screenshots against the reference and repair genuine mismatches. New API has not yet had real frontend-to-backend browser verification or production-volume performance checks. Full goal also retains the broader action-scope/release-configuration/owner-provisioning blockers recorded above.

### Overview verification completed

- Final production build and standalone packaging **32786** exited 0. The earlier build also produced a working snapshot, but its build-time proxy still targeted 5000; the real-API browser attempt was stopped and the final build correctly targets disposable loopback 5011. No real session or mutation was used.
- Final frontend unit run **49603** passed **116/116** across 15 files. An earlier run passed 105 cases but failed to start one worker; the affected file then passed and the complete final run succeeded. Three new refresh tests found and verified a fix for immediately rejected requests: `useResource` exposes the settled request revision so identical consecutive errors still schedule the next backoff. New date ranges reset the failure count. Pending requests do not receive another timed refresh; hidden/offline polls pause.
- Final browser batch **8688** exited 0 with **56/56**: 12 overview fixture cases, four real disposable API/proxy cases, 13 navigation, 13 cart/operations and 14 public header cases. All four roles, 320/360/390/768/1024/1440/1655 widths, axe/overflow checks, date validation/error/empty/retry, legend/day/table controls, MFA routing and late-response account isolation passed. Real cases use synthetic test sessions, not real credential/MFA enrollment.
- The first 38-case browser batch passed 32 and exposed mobile overflow plus an ambiguous test alert selector. Overflow came from an absolutely positioned screen-reader table label escaping the scroll region; positioning that region fixed it without hiding page overflow. Date errors now focus and describe the end-date input. The test selects the explicit date error rather than Next.js's route announcer. All six former failures passed in the final run.
- Inspected reference and final 1655px, 1440px and 390px screenshots with view_image. Comparison: dark 300px sidebar at reference width; compact branding and grouped links; white search/account header; five peach/green/blue/teal/pink cards; approximately 2:1 chart/status and table/order grids; rounded white bordered panels; Quicksand and semantic status chips. SVG height and table spacing were reduced after the first comparison. Mobile stacks panels, keeps two card columns and confines table scrolling to its labelled region. The page is taller than the source image because it includes explicit date controls, accessible chart exploration and five/four recent records; no pixel-perfect claim is made.
- All **310** app/lib/public/script/configuration source files match the final build snapshot (zero differences). Full frontend formatting passed. Focused frontend type/lint and backend lint passed; full frontend lint **9690** passed (terminal exit 0). Backend build/format/schema validation/no-drift and the 15-case API batch passed earlier in this stage.
- Coverage is **231 operations / 193 paths**. `overview-browser-verification.md` documents the guarded harness and build-time proxy requirement. New index migration remains applied only to the disposable database. Production-scale query timing has not been measured.
- Owned runtime handles: frontend **62289** at 3001 and disposable overview API **45473** at 5011; stop both after verification. No real owner provisioning, outgoing message, payment, commit, push or deployment occurred. The full goal is still active, including the remaining backend issues in `release-blockers.md` and the approved owner account's absence from the real local database.

- Owned overview frontend/API servers **62289** and **45473** were stopped after the 56-case browser verification. User port 3000 remains untouched. Next bounded backend task: reproduce and fix the documented manual-payment review race without weakening financial settlement rules.

### Payment decision and recovery follow-up

- Reproduced simultaneous manual review reads against real disposable PostgreSQL using a Prisma read barrier. The existing `ManualPaymentReview_protect_terminal` trigger already prevents contradictory committed decisions; the defect was that the losing service transaction raised an unhandled database error instead of an expected conflict. `reviewManual` now conditionally updates only PENDING and returns 409 when another reviewer won. No database trigger, separation-of-duties check, payment verification or settlement invariant was relaxed.
- New `manual-review-concurrency.test.ts` passes approval/approval and approval/rejection races. Both read PENDING before proceeding; exactly one reviewer wins, one decision audit is recorded, payment/attempt state matches that winner and at most one capture is written. Real settlement writes are retained; only the two reads are synchronized. No provider or real transfer is involved.
- Reproduced original-key payment-intent recovery returning 404 after a paid order moves to PROCESSING. `createIntent` now checks the actor-bound key through a customer-scoped repository query before resolving mutable source eligibility for a new request. Purpose and target must match; stored amounts remain authoritative. A new key still performs current payable validation. No schema or response shape changed.
- The extended real API flow verifies original payment recovery, 409 for a changed target, 404/no data for another customer with the same raw key, and rejection of a new payment for the progressed order with only one payment record remaining. An initial implementation attempted to read customerId from the intentionally minimal projection; the regression caught that, and ownership is now enforced directly in the query without broadening the response.
- Final focused payment/Monnify/finance/manual-race batch passed **12/12** across four files. The earlier finance/support-worker batch passed **15/15**. Backend typecheck, lint, build and full format check passed after the final implementation. A full disposable backend regression is now running; collect its actual terminal result before updating final totals. Owner provisioning cases are separately gated.
- No frontend runtime changed after the verified 310-file production parity check. API coverage remains 231 operations; ledger and release notes now distinguish the resolved manual-review/recovery findings from remaining distinct-key creation, manual evidence replay, expiry, conditional cart/catalogue/team writes and other release work. The full goal remains active.

### Final verification and next continuation

- Full disposable backend run **74285** completed: **195 passed, one failed, three skipped** across 50 files. Its sole failure was the team test fixture generating `expiresAt` and `idleExpiresAt` from separate clock reads, violating `aat_session_idle_expiry_valid` by one millisecond. The helper now reuses one timestamp. The full affected team file then passed **12/12** as **27344**, followed by successful backend lint. This yields **196 distinct passing backend cases** across the full run and corrected-file rerun; the three owner-provisioning cases remain separately gated and were not rerun in this stage.
- Backend types/build/format passed after both payment fixes; the subsequent change is fixture-only and formatted. Frontend full lint/type/format, **116 unit tests**, final production build/packaging and **56 production browser cases** passed. Four browser cases used the actual disposable API and database. Original owner email remains absent from the real local database; none of these synthetic tests provisioned it.
- All owned servers, builds and test runners are terminal. Loopback 3001/5011 have no listeners. The developer's 3000 server remains untouched. Runtime source parity remains 310 files with no differences; subsequent frontend edits are documentation/test-only.
- Continue the full authorized goal from `release-blockers.md`: prioritize bounded reproductions of manual evidence replay and unsafe operator expiry, then atomic cart/catalogue/team write guarantees and remaining operational permission/coverage gaps. Distinct-key payment creation requires a coherent payable-level policy, not a browser-only lock. Reassess older findings against current authorization: STAFF refund access is now denied before replay, so the previous branch-replay concern predates that boundary. Keep unsupported business policies and real owner provisioning pending verified prerequisites.
- The shared reference overview and header routing stage is complete within the documented checks. The full application goal is not complete or release-approved; production configuration, remaining backend gaps and production-scale performance are still outstanding. No real messages/payments, deployment, commit or push occurred.

## Updated objective and active September 18 follow-up

- The revised objective at `d6f51d1d-fe41-4116-ac42-5d5f9bc8a97a/goal-objective.md` was read fully. It retains the complete dashboard brief and adds a specific requirement to fix mobile support/navigation controls appearing on laptops. Prior explicit Quicksand/owner/STAFF answers still apply. This is the active priority; the full goal remains active.
- Reproduced manual evidence replay failure after a committed submission's upload ticket expires. `submitManual` now establishes customer ownership, checks the original fingerprint before evidence verification, and rechecks replay inside the payment lock after verifying a new upload outside the transaction. Changed payloads, other customers and invalid/expired first submissions remain denied. Four new disposable database cases pass, including forced concurrent object verification and one committed attempt/audit. The prior evidence/review/Phase 8 batch passed six cases before adding the fourth test; final type/lint/build/format is running.
- The customer manual-payment form now offers explicit original-body/original-key recovery with evidence. Its pre-first-attempt expiry guard remains; an uncertain attempt may reach the server's owned replay after ticket expiry. Evidence/body edits stay locked. The existing configured-storage browser scenario was updated to test expiry/recovery and exactly one upload. Production browser verification is pending.
- Found the support control media query used max-width 1279px OR max-height 500px, which selected the mobile bottom bar on laptops. It now applies below/equal 850px, or on coarse-pointer landscape devices below/equal 1023px and 500px high. Desktop uses the existing two floating icon buttons. Focus reveal now checks actual control/field rectangle overlap, so both the mobile bar and desktop icons avoid covering focused fields.
- An open public mobile menu previously remained visible after widening the viewport. CSS hides it above 1050px and a media-query listener closes it, moving keyboard focus to desktop navigation if focus was inside the disappearing menu. Existing role-aware account links remain unchanged.
- Added 13 support-responsive browser cases: 320/360/390/768/850/851/1024/1280/1366/1440 plus short laptop, mobile-to-desktop resize/focus and touch landscape. Public account-recovery tests now check the correct mobile boundary and hit-test controls on laptops too. All new browser cases still need to run.
- Frontend type/focused lint passed before the final support overlap refinement. Snapshot production build **8609** is running with loopback backend 5011 and synthetic storage.invalid allowlisted only for isolated upload tests. No owned runtime is started. Collect this exact handle; do not restart based on observation timeout. After build, start 3001 with the same storage allowlist and run support-responsive, header-session, manual-payments (RUN_ASSET_BROWSER_TESTS=true), and gated public-account-actions (RUN_PUBLIC_ACTION_TESTS=true; owns its read-only fixture server5011). Inspect mobile/laptop screenshots before claiming the user's new requirement complete.

### Latest sidebar requirement and completed support/payment checks

- Read the new controlling objective `65c2e449-ba40-49b8-a627-caf6bb76d730/goal-objective.md` in full. It explicitly requires icons to remain visible when collapsed and no visible scrollbar anywhere inside the sidebar. This supersedes the earlier option to style the scrollbar.
- Support/header/manual-payment/public-account production browser batch **16567 passed 66/66**: 14 header-role/session cases, 12 manual-payment cases (including configured evidence recovery), 27 public-account/action cases and 13 support-responsive cases. The 390x844, 1024x768 and 1366x480 support screenshots were inspected: mobile bottom bar and laptop floating icons are correctly separated. No actual upload, provider payment or outgoing message occurred.
- Final backend evidence replay checks **84792 passed 4/4**; backend types/lint/build/full format **98537 passed**. The API inventory remains 231 operations and now records evidence replay recovery. Stale draft-quotation and missing-aggregate blocker rows were reconciled against the implemented and previously verified server behavior.
- Sidebar work in `dashboard-shell.tsx`, `dashboard-navigation.tsx`, and `globals.css`: accessible desktop collapse/expand button; 80px compact icon rail; grouped icons expand and focus their native disclosure; role-filtered direct icons keep working links. Navigation retains wheel/touch/keyboard scrolling while both standard and WebKit scrollbars are hidden. Brand and sign-out stay fixed in the viewport on short laptops. Mobile transition restores expanded navigation and relocates keyboard focus when a control would disappear.
- Added responsive boundaries (320, 850, 900, 901), four role-specific compact rail cases, resize/focus and short-laptop scrolling checks. These new sidebar cases are NOT verified yet. Production snapshot build **21515** is running after successful formatting/typecheck/focused lint; collect its exact terminal result before starting 3001. Earlier snapshot build8609 passed, and its runtime92179 was stopped after the 66-case batch. User port3000 remains untouched.

### Verified sidebar and catalogue follow-up

- Final production snapshot **21515 passed** (including standalone packaging); frontend types and focused lint passed before it. Browser batch **46182 passed 48/48**: 23 navigation/sidebar cases, 12 overview cases and 13 repeated support cases. Together with batch16567 this is **101 distinct passing browser cases**, with 13 support cases run on both builds.
- Inspected collapsed STAFF laptop, expanded short-laptop navigation, mobile390 and desktop1655 overview screenshots. All permitted primary icons remain visible in the compact rail; group icons expand/focus their disclosure; no scrollbar is displayed inside the sidebar, while native scrolling still works. Desktop/mobile transitions preserve usable keyboard focus. Reference composition and Quicksand remain intact. Screenshots contain isolated fixtures, not production business data.
- Runtime snapshot comparison found **306 app/lib/public/configuration files identical**, zero differences. Owned runtime **10573 was stopped** after verification. No changes to user port3000 or production environment.
- Reproduced catalogue partial-year PATCH returning500 against PostgreSQL (11688). Existing database checks already prevented invalid committed ranges; the issue was missing merged validation in the service. `CatalogService.updateCompatibility` now reads the current bounds inside the existing product lock, preserves explicit null and omitted values, returns404 for an absent child and409 for an inverted merged range. No schema or invariant changed.
- Expanded Phase4 real-API regression covers invalid partial bounds, unchanged stored values on denial, concurrent competing bounds with one200/one409, nullable ranges and missing children. Final catalogue/API/policy batch **98218 passed8/8**, followed by successful backend typecheck/lint/build. Full backend formatting also passed. API ledger remains231 operations.
- Frontend full lint and **116 unit cases across15 files** have passed in **9316**; full formatcheck is still running. Order-expiry reproduction **84287** is running against the disposable overview test database; only candidate discovery is isolated, with real locks/payment reads/reservation releases/audits. No expiry implementation has changed yet. Preserve and collect both exact handles.

### Expiry protection and final checks for this increment

- Order reproduction84287 confirmed manual expiry released reservations during committed manual review and provider processing; two additional initial failures were a fixture assertion using `actorUserId` instead of the stored audit `userId`. `OrdersService.expireDue` and `expireDueSystem` now call the same private locked eligibility routine. Existing payment exclusions and deadline/status rechecks are shared, while operator identity and request context remain in the audit. No new expiry policy was introduced.
- Seven new order cases verify unpaid release once, committed review/processing protection for both entry points, extended-deadline recheck and simultaneous operator/worker release with one inventory movement/audit. Batch **31888 passed15/15** (order expiry, commerce and support workers), followed by successful backend type/lint/build/full format.
- Vehicle reproduction initially exposed a fixture missing required synthetic terms; after correction, it confirmed manual expiry bypassed payment protection and deadline extensions, and failed on candidates already expired by a concurrent worker. `VehicleSalesService.expire` and `expireSystem` now share their existing locked worker eligibility implementation. The generic transition no longer accepts an internal expiry bypass. Operator identity is retained in history/audit, and branch authorization remains enforced.
- Nine vehicle cases cover unpaid release once, committed review/processing protection, extended-deadline rechecks for both paths and worker/operator racing with one history/audit. Final batch **83944 passed36/36** (vehicle/order expiry plus vehicle API/security/flow), followed by successful backend typecheck/lint/build/full format. No real listing, order or payment was changed.
- These expiry tests do NOT establish correctness for all interleavings with newly initializing/submitting/settling payments. Candidate transactions still commit separately, so partial-batch recovery remains a release issue. Browser bulk-expiry actions remain unavailable until those separate concerns are resolved. API coverage and release findings now reflect the completed parity fix instead of the old missing-check claim.
- Frontend full lint and **116 unit tests** passed9316. Its format check found only regenerated api-coverage.json; after formatting that generated file, full frontend formatcheck **65073 passed**. Final runtime/build-script parity is **310 files, zero differences**. Owned 3001/5011 listeners are absent; developer3000 is untouched.
- Full goal remains active. Continue from the remaining release list: expiry versus new payment/settlement interleavings and partial-batch outcomes, distinct-key payment/attempt creation, atomic cart/catalogue/team revisions, processing-job pagination, remaining real storage/provider/email configuration and production-scale verification. The selected owner must exist and be verified before provisioning; do not auto-create or auto-verify it. No commit, push, deployment or real outgoing message/payment occurred.
- Final read-only owner inspection again returned ownerCount0, candidate null, eligible false, applied false for alliedautotechltd@gmail.com in the configured local database. No account was created, verified or promoted. All builds, test runners and owned servers from this increment have completed or stopped.

## September 18 continuation: settlement versus expiry

- Re-read the controlling65c2e449 objective and inspected the current dirty worktree. Previous goal turn was verified progress; the full goal remains active. No old process was restarted; all previous handles were terminal.
- New `backend/tests/integration/settlement-expiry-race.test.ts` holds an actual order/vehicle transaction row lock in the expiry transaction, starts provider verification, observes contention via PostgreSQL pg_blocking_pids, then lets expiry commit. Only candidate discovery/lock scheduling and the external provider result are isolated; all settlement, expiry, audit and ledger writes are real disposable PostgreSQL.
- Initial fixture needed a createdAt before its expired payment deadline. After correction, both real reproductions failed because LATE_SUCCESS anomalies were missing: settlement read the source before obtaining its write lock and continued using the stale state after expiry committed.
- `PaymentsService.settle` now locks the Order or VehicleTransaction row BEFORE reading its lifecycle state. Booking already had a source lock, but its lock ordering relative to ExpirationWorker needs a separate audit. No schema, payment verification, amount check or late-success rule changed.
- Batch **23508 passed22/22** across settlement-expiry races, seven order-expiry cases, nine vehicle-expiry cases, manual-review concurrency, Phase8 and Monnify flows. Backend typecheck and lint also passed in that terminal0 handle. Backend build/format remain to run after the next payment change. No frontend runtime changed after the prior310-file production parity check.
- Next concrete investigation: provider verification can finish before initializeOnlineProvider receives its initialize response. Finalization currently unconditionally writes attempt PENDING; this can fail on or overwrite a provider-terminal state. Reproduce using a local provider adapter that verifies inside initialize; preserve terminal outcomes and return a recoverable409 rather than downgrading them. No source changes for that issue yet.
- Additional inspected concern: booking expiry takes Booking then Payment locks, while settlement currently takes Payment then Booking. Reproduce and align the lock order before claiming all payment/expiry races verified. Existing order/vehicle expiry batch checks do not prove this booking interleaving.

### Checkout finalization and root provisioning shortcut

- Added `payment-initialization-race.test.ts`. Before the fix, early SUCCESSFUL verification caused a database immutability error; early FAILED and ABANDONED results were overwritten as PENDING. The pending case already worked. `initializeOnlineProvider` finalization now locks attempt then payment, rechecks current payment/deadlines, rejects terminal attempts with409 and preserves active status when storing checkout state. Verification and database protections remain unchanged.
- Final batch **85193 passed10/10** across four initialization-race cases, two settlement-expiry cases (including repeated verification), Monnify, Phase8 and booking-deposit flows. Backend typecheck/lint/build/full formatting passed. Earlier **63743** also passed the two strengthened settlement tests and build/format. These are isolated provider adapters and disposable PostgreSQL, not real payments or notifications.
- User reported `npm run bootstrap:super-admin` failing from the parent directory. There was no root package.json; the script existed only in backend/package.json. Added a dependency-free private root package with a forwarding script. The backend CLI now prints usage with no arguments/--help and dynamically loads database configuration only after argument validation. Mutation still requires explicit --apply, selected email, exact expected user ID, installed protection and an active verified account.
- Root command and root inspection with alliedautotechltd@gmail.com both passed in **5314**. Current inspection still reports ownerCount0/candidate null/eligible false/applied false. No real account was created, verified or promoted. Documentation now supports running from root or backend. Script TypeScript check and backend lint **22114 passed** after the CLI edit.
- Next substantive payment task: reproduce the Booking-to-Payment versus Payment-to-Booking lock inversion in ExpirationWorker/PaymentsService settlement. The order/vehicle race fix does not claim to cover booking expiry. Then address distinct-key active attempts/payable intents and the other documented release requirements. Full goal remains active; no required work was redefined away.
