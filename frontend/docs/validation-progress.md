# Validation progress

This is an interim record, not release approval. Full acceptance remains the supplied 18-section brief.

## September 17 staff administration increment

- Implemented the staff directory, private account details, reviewed status/branch/role changes, invitation creation and invitation acceptance. Seven endpoint mappings were updated; the inventory remains 225 operations. The backend organization schemas, safe selectors, role/self restrictions, advisory locks, session revocation, invitation replacement and response projections were inspected.
- All 15 initial development Edge scenarios passed in one batch (2.8 minutes). They cover staff exclusion with zero administrator reads, failed-list recovery, filters/cursors, self/peer/super-administrator protection, stale preflight rejection, exact status/branch/role contracts, active-branch paging, invitation review/queued receipts, fragment scrubbing, password/server-field validation, unknown outcomes and cross-tab credential clearing.
- Review found that a failed preflight read incorrectly locked the form as an uncertain write. It now reports that no mutation was submitted and permits recovery; changed records still require review, and malformed/contradictory write responses remain locked. Four affected development scenarios passed together (24.5 seconds), including that recovery, contradictory success handling, mobile account accessibility and staff acceptance copy. Browser coverage now contains 17 staff scenarios.
- The full lint command found one React refs/callback-placement error in the acceptance form. Moving submission handling into the event handler fixed it; targeted lint passed for that component and all subsequently changed source/test files. TypeScript, all 57 unit tests and full formatting passed in their recorded commands.
- Production compilation, TypeScript and standalone packaging passed in the local copy outside OneDrive, including a fresh build after the preflight fix. All 238 runtime/configuration/public/packaging files matched the workspace by SHA-256. The existing development server was left running; the isolated production test server uses loopback port 3001.
- All 27 production Edge scenarios passed in one batch (40.2 seconds): 17 staff, seven account and three existing authentication/navigation scenarios. Mobile overflow and axe WCAG 2.2 AA checks passed for account management and invitation review. The acceptance flow captured no page errors. Screenshots were visually inspected at 320 by 740 and 1280 by 900: `allied-staff-invitation-mobile.png`, `allied-staff-account-mobile.png` and `allied-staff-acceptance-desktop.png` in the OS temporary directory. No development overlay appeared. The final wording-only cleanup removed implementation details from the invitation/password instructions. A fresh production build/standalone package passed; all 238 runtime and packaging files again matched the workspace. Both affected production browser scenarios passed (7.3 seconds), and the updated screenshots were inspected.
- API traffic is intercepted: no real invitation, account creation, role/status change, notification or session revocation was performed. Real administrator MFA, email delivery, token consumption, database audits/revocation and concurrent access changes remain release checks. The missing atomic expected-version contract is documented as a backend dependency.

## September 17 audit history increment

- Added the administrator audit screen and one endpoint inventory entry, preserving the 225-operation inventory. Actual backend query, selection, cursor order, 90-day two-sided range rule and privileged read logging were inspected. Nullable actors remain unknown, joined roles are labeled current roles, and JSON snapshots are labeled partial values.
- Six development Edge scenarios passed in one batch (55.3 seconds), covering staff denial with zero audit reads, failed-read recovery, cursor navigation, all private reference filters, field focus/date validation, exact Lagos offsets, lazy escaped JSON with null/false/zero/empty strings, keyboard scrolling, mobile overflow/accessibility and session invalidation. Initial assertion failures were test assumptions: the Next route announcer was also an alert, and development mode performed duplicate initial reads. Assertions were corrected without relaxing the behavior under test.
- Lint, TypeScript and all 57 unit tests passed. The combined check stopped during production cache cleanup with a Windows EPERM on a generated route directory. Old production artifacts were preserved under the OS temporary directory (`allied-build-preserved-audit-e44f60d5c0444d13adb54f0cec63b9e3`); Move-Item reported permissions on three generated type subdirectories, but a subsequent build proceeded through successful compilation and type checking. The already-running development server was left running.
- A second workspace build compiled and type-checked successfully but failed during trace packaging with EBUSY on a generated refund route file. An isolated source/dependency copy outside OneDrive was then prepared at `allied-audit-build-c625ba14b7ba4854a2a0112cba8b75b2` in the OS temporary directory. All 217 application/configuration files plus nine public/packaging files matched the workspace by SHA-256. The isolated production build, TypeScript and standalone packaging passed.
- Three additional development checks passed (40.2 seconds): an administrator lacking verified MFA made zero audit reads before redirect; the updated touch targets/JSON regions passed axe WCAG 2.2 AA tags and keyboard/mobile checks; unsupported or invalid current-record links were absent.
- All 24 production Edge scenarios passed in one batch (59.6 seconds): seven audit, ten operational queue and seven account flows. Audit tests cover administrator MFA redirect before reads, staff restrictions, exact queries/cursors, failed reads, date validation, escaped lazy JSON, keyboard scrolling, account cleanup and axe WCAG 2.2 AA tags. Production screenshots at 320 by 740 and 1280 by 900 were inspected; a capture-only desktop scroll-position correction avoids a misleading fixed-sidebar position in the full-page image. The capture-only follow-up passed (10.7 seconds) and its corrected screenshot was inspected. Evidence: `allied-audit-mobile.png` and `allied-audit-desktop.png` in the OS temporary directory. No development overlay or captured audit page errors appeared.
- Full formatting passed before the final documentation/capture changes; the final changed-document/test formatting check also passed. The Windows workspace build remains affected by generated-file locks; the passing standalone artifact is in the verified local copy described above.
- The screen is read-only; browser API traffic is intercepted. Real administrator MFA, database authorization and audit-read event persistence remain unverified.

## September 17 processing jobs, exceptions and disputes

- Added three administrator screens and five API integrations for processing-job reads/retries, payment-exception reads/decisions and dispute reads. Overview links connect real queue/exception metrics to these screens. The inventory remains 225 operations; unmapped and unfinished work is still tracked separately.
- Nine initial development browser scenarios passed; the added overview navigation/zero-count scenario then passed separately. The full frontend check passed lint, TypeScript, all 54 unit tests, the production build, standalone packaging and formatting. Backend source was unchanged.
- All 27 affected production Edge scenarios passed in one batch (53.1 seconds): ten operational queue scenarios, ten staff payment/refund scenarios and seven account flows. These verify administrator-only reads, staff denial without API requests, failed-read recovery, source/type/status/category filters, working anomaly/dispute cursor resets, exact job attempt guards, stale draft reload, explicit notification/financial consequence review, unknown/malformed response locks and both exception closure outcomes.
- Dispute checks cover exact large kobo amounts, Lagos response deadlines, absent deadlines and evidence presence without unsupported attachment/decision buttons. Job tests verify no raw backend exception text appears and no cursor request or misleading Next control is offered when the backend ignores pagination.
- Production screenshots were inspected at 320 by 740 and 1280 by 900: `allied-exception-review-mobile.png` and `allied-disputes-desktop.png` in the OS temporary directory. Exception and dispute mobile overflow/axe WCAG A/AA checks passed; the dispute flow captured no page errors. The confirmation heading, record facts and controls remained readable, with no development overlay in production.
- All consequential calls were intercepted with synthetic fixtures. No real worker retry, notification, payment or dispute action was performed. Real administrator/MFA authorization and backend concurrency/worker verification remain outstanding. The jobs endpoint's ignored cursor and missing dispute decision/evidence routes are explicitly documented limitations; this increment does not certify complete history access or full application release readiness.

## September 17 customer manual payments, shared uploads and confirmation layout

- Implemented the two customer manual-payment/evidence endpoints and updated the 225-operation inventory. Domain-specific preparation adapters now share upload transport and UI with vehicle evidence. The backend was inspected for actual body, token, ownership, expiry and replay behavior; backend source files were unchanged.
- Customer coverage includes bank transfer/POS/cash, required payer/date validation, Lagos timestamps, exact server request amounts, cancelled-review field retention, corrected details after a rejected attempt, optional evidence, pending/terminal/deadline gates, original-key/body recovery without evidence and locked unknown evidence submissions. Receipt file limits, token-kind rejection, exact bytes/checksum/signed headers, no application credentials on storage PUT, no persisted tickets and session invalidation during preparation are covered.
- Development testing found a selected-file loss during refresh after evidence expiry. The form now preserves the file and fields through temporary refreshes. Eleven development scenarios passed together after that fix (53.2 seconds); the added session-invalidation scenario passed separately (7.6 seconds including runner startup). Its initial failing assertion expected a login heading, while the application correctly showed the session revalidation boundary; the test now checks that actual boundary and absence of storage transfer.
- Visual inspection found that long mobile confirmations opened below their heading. The shared mutation dialog now keeps its heading, error feedback and actions visible while a named, keyboard-focusable details region scrolls. Production browser assertions cover visible heading/actions at 320 by 740, Shift+Tab focus, End/Home scrolling, page overflow and axe WCAG A/AA checks. Production mobile confirmation and desktop review-state screenshots were inspected: `allied-manual-payment-mobile.png` and `allied-manual-payment-review-desktop.png` in the OS temporary directory.
- Lint, TypeScript, all 54 unit tests, production build/standalone packaging and formatting passed across the recorded commands. The combined check reached a formatting-only failure in the new unit-test file; formatting was corrected and the full formatting check passed. The later homepage color edit passed targeted lint/formatting and a fresh production build, including TypeScript and standalone preparation.
- Full configured-storage production Edge run: 76 passed, four failed and two skipped (4.3 minutes). All customer payment, account/authentication, inventory, invoice, workshop, refund, inspection, slot, vehicle stock and handover scenarios passed. The four failures were the same existing homepage paragraph contrast issue at 320/768/1024/1440 widths: muted text had a 4:1 ratio against navy. Its color was corrected without weakening the accessibility assertions.
- Fresh production build with storage disabled: all nine targeted scenarios passed (26.1 seconds), covering all seven public tests, customer manual submission without optional evidence and unavailable handover upload controls. This verifies **81 distinct browser scenarios across runs**, not one clean 81-test batch. The real database journey remains intentionally skipped because its isolated harness was not running; the configured run's unavailable-storage scenario was covered in the final separate run.
- API and storage traffic for financial mutations was intercepted with isolated fixtures. No real payment/refund or notification was sent. Real customer/staff authorization, database concurrency, actual storage/CORS and test-mode provider verification remain required. Evidence replay-before-expiry validation and concurrent active-attempt rules are documented backend findings, not frontend guarantees.

## September 17 staff payments and refunds increment

- Implemented six additional inventory operations across payment records, manual evidence/review, refund requests/decisions and the administrator refund queue. The inventory remains 225 operations; coverage does not imply every operation is complete.
- Ten development Edge scenarios passed in one batch (44.5 seconds). A hidden cancellation-note validation issue was fixed before this batch: approval ignores and omits cancellation-only notes, while cancellation validates and trims them.
- The complete frontend check passed lint, TypeScript, all 52 unit tests, the production build, standalone packaging and formatting. Backend source files were unchanged.
- All 22 affected production Edge scenarios passed in one batch (40.7 seconds): ten payment/refund, five invoice and seven account flows. Tests cover exact-kobo limits and original-key/body refund replay; explicit private evidence access; role restrictions and zero administrator refund reads for staff; different-operator decisions; required review notes; cancelled, pending, attention and succeeded states; failed-list recovery; filter/cursor reset; and no automatic resend after interrupted manual/refund decisions. Existing checkout, payment-verification and account-isolation regressions passed.
- Production screenshots at 320 by 740 and 1280 by 900 were visually inspected: refund review actions and amounts remain visible, and the attention state clearly calls for reconciliation. Evidence is in the OS temporary directory as `allied-refund-request-mobile.png` and `allied-refund-attention-desktop.png`. Mobile overflow, axe WCAG A/AA, modal focus and captured page-error assertions passed. No development overlay appeared in production evidence.
- These tests intercept API/storage traffic and use isolated fixtures. They do not verify real staff MFA, backend branch authorization, database financial concurrency, storage content or actual provider refunds. The manual-review race, refund replay authorization gap, missing offline completion contract and remaining customer manual submission/upload are documented release work. No real financial action or notification was performed.

## Verified checks

- Discovery baseline: frontend lint, typecheck and production build passed. Frontend unit command initially failed because there were no tests. Backend lint/typecheck passed; 126 tests passed and 26 database-dependent tests were skipped.
- API client, money and media unit suite: 36 tests passed. Covers no automatic mutation replay, concurrent CSRF rotation, late account/CSRF responses, safe error mapping, path traversal rejection, hosted checkout URLs, exact integer money conversion and media host boundaries.
- Public browser suite: 5 tests passed in Edge with isolated browser API responses. Home and automated guide at 320/768/1440 pixels; axe WCAG A/AA checks, no page overflow, help search recovery and service failure/retry.
- Initial account browser suite: 5 of 6 passed. Uncertain checkout reused the original body/key and made no payment request; pending verification hid new checkout; failed orders remained an error; account invalidation removed prior data; administrator category creation required review and sent the expected contract.
- The sixth account assertion observed development Next.js returning `no-cache, must-revalidate` for `/admin/categories`, despite proxy `no-store`. Production cache verification is still required. This result does not establish a production cache leak; role assertions after that failing line were not reached.
- Follow-up production run: all 11 browser tests passed, including the unchanged strict `no-store` header assertion and customer redirect away from administration. Production build, lint and typecheck passed before subsequent staff order/profile edits. Browser tests still use intercepted API fixtures, so backend authorization is verified separately.
- Fresh PostgreSQL 17 Alpine container, bound only to `127.0.0.1:55432`, database `allied_frontend_test`: all 30 backend migrations replayed successfully. All 152 tests passed in 44 files with database suites enabled, one worker, and email/SMS delivery and real payment providers disabled by test setup (403.71 seconds).
- Real frontend proxy journey passed against the guarded loopback test API: password login, HttpOnly SameSite=Lax local session cookie, server HTML containing service/product headings, booking response with server-rounded 302-kobo deposit, cart quantity, checkout total of 250000 kobo, and logout cookie removal. This does not validate production Secure cookies over HTTPS or live providers.
- All three authentication/form/navigation browser tests passed after scoping the registration alert check to the main content: successful registration clears the password, the verification token leaves the fragment without an automatic submission, and Escape closes mobile navigation and restores focus.
- Full frontend check passed lint, typecheck, 36 unit tests, Webpack build and standalone asset preparation. Its final formatting gate identified only `vitest.config.ts`; formatting was corrected and the formatting check passed. Subsequent staff dialog refinements need affected validation.
- Repeat real API journey passed with a fresh fixture and added profile edit, including backend validation and successful response parsing. Fixture setup now creates new isolated records per request, so the journey can be rerun without reusing an occupied slot.
- Both public authentication references rendered successfully in anonymous Edge contexts. Visual inspection completed for Link's compact login form and Termii's split sign-in layout; evidence stored as `allied-reference-link-loaded.png` and `allied-reference-termii-loaded.png` in the OS temporary directory.

## Staff workshop increment

- Three additional Edge browser fixtures passed: staff no-show review and lost-response reconciliation; readable legacy customer booking with a missing branch and service duration; quotation issue and draft work-order creation followed by approval, progress, quality check and completion.
- Requests assert quotation revision and work-order versions. Completion refreshes the parent booking. Checks include mobile overflow, dialog keyboard focus and axe WCAG A/AA results. These are intercepted fixtures, not a real staff database journey.
- A failing first pass exposed asynchronous staff choices clearing the existing assignment. The picker now controls and preserves the selection across option replacement. The same pattern was corrected in administrator record lookups, with previous/next navigation; affected admin validation remains required.
- Follow-up line editing: quotation create/replace now supports catalogue parts, labour, fees, tax, notes and Lagos-time expiry. Work-order items can be appended after draft creation. Inventory, vehicle operations, refunds and other staff domains remain outstanding.
- A service duration and a booking branch can be null in real serializers. Runtime schemas now accept both, and public service details omit an unset duration.
- Production build initially rejected `String.raw` in the proxy matcher because Next.js requires a statically readable constant. An equivalent escaped string literal fixed the build. Build, standalone packaging, lint, TypeScript, 36 unit tests, formatting and whitespace checks passed for this increment. The preserved generated workshop image is absent from the standalone public directory.
- The subsequent production browser run passed 17 of 18 checks, including strict private cache headers, all workshop checks and administrator lookup paging. The remaining mobile-navigation check encountered a browser protocol/session-closed error; its snapshot showed the menu closed and the trigger focused. It passed unchanged on a targeted rerun in 2 seconds. This is recorded as a test-runtime interruption, not a clean single-run 18/18 result.
- Subsequent development browser checks passed: quotation create/replace sends exact integer kobo, omits client pricing for PART lines, retains Lagos expiry and sends the original draft revision; a lost creation response shows the saved draft after refresh and locks the original form; work-order additional fee entry sends exact amount/version before milestone progression. Production packaging and mobile editor validation for these later changes remain separate checks.
- Quotation editor at 320 pixels passed page-overflow and axe checks; its screenshot was inspected. Pagination and native date-input sizing were refined afterward. Runtime logs exposed duplicate sibling keys between work-order notes and item forms; distinct keys and a browser warning assertion were added. The full frontend check then passed lint, TypeScript, all 36 unit tests, Webpack build, standalone preparation and formatting. Later slot and quotation-editor reconciliation changes require affected production validation.
- Two staff slot browser fixtures passed after correcting validation focus to follow visual field order: slot publication and versioned closure, and a saved creation whose response is lost. They verify selected staff-profile ID, Lagos time, eligibility filtering, another staff member's restricted controls, unknown-outcome lock, 320-pixel overflow and axe checks. These tests intercept API responses and do not replace real database/MFA authorization verification.
- Latest complete frontend gate passed lint, TypeScript, 36 unit tests, production Webpack build, standalone preparation and formatting after slot implementation. The subsequent production browser run passed all 22 tests in 1.5 minutes across account, authentication, public, workshop and slot suites. The mobile quotation-editor screenshot was inspected again with contained pagination and date controls. These results do not cover the unfinished domains listed below.
- An additional targeted production test passed lost-response draft replacement: the refreshed new draft and voided old draft remain visible, and create/edit controls recover. This extends the browser suite to 23 tests; the last full batch ran 22, followed by this separate passing check. Test lint and formatting passed after the addition.

## Inventory increment

- Implemented inventory list/detail, low-stock/branch/part filters, movement history, reservations, administrator empty-record creation, versioned reorder level, stock movements, new reservations and release.
- Four development browser scenarios passed across initial and targeted runs: lost stock-in response with unchanged displayed balances until refresh and preserved idempotency key/body; reserve/release/exact-quantity reserved sale; administrator creation and reorder update; low-stock/history failure/rejected-movement recovery. Initial cold development navigation/session hydration exceeded the test timeout; the affected scenarios passed on unchanged warm reruns. Later branch/part filters and production packaging still require affected checks.
- A rejected or cancelled review now preserves stock movement/reservation draft input. Confirmed inventory changes reset forms and refresh balances, history and reservations. Unknown stock operations pause new mutations and retain the original safe retry proposal.
- The shared insufficient-stock message now applies to staff and customers. No stock quantity is optimistically confirmed, and stock sales are explicitly separate from orders, invoices and payments.
- The subsequent complete development inventory batch passed all four tests (1.2 minutes), including branch/part filters, populated movement history and history/reservation status filters. The 320-pixel movement form screenshot was inspected with contained controls and readable labels. Production verification remains separate.

## September 17 design and billing increment

- Re-read the updated worktree and retained its red/navy/white visual direction. Quicksand now supplies the heading and UI font; Nibble is preserved on disk but not loaded. Removed the legacy `.grid` class collision with Tailwind's responsive grid utilities, corrected the garage destination and replaced unavailable legal-page links with existing support destinations.
- Homepage sample parts/vehicles were replaced with real public catalogue requests, server prices, approved media and distinct empty/error states. Placeholder workshop media remains explicitly labelled; approved photography and approved legal content remain dependencies.
- All five public browser tests passed after correcting a red-on-navy label contrast failure. A separate 1024×600 landscape test then passed, extending public coverage to six tests. Homepage screenshots at 320 and 1440 pixels were inspected. These tests intercept catalogue responses and do not establish real catalogue or image-host availability.
- Staff invoice creation, list/status filter/cursor controls, detail, issue and void are implemented. Sources are existing orders, accepted booking quotations and eligible vehicle transactions. Amounts, deposit credit and payment states come from the server. Creation has explicit review, Lagos due dates and an uncertain-outcome lock that survives hiding/reopening the form.
- The booking invoice creation/credit/issue/void browser scenario and failed-list/payment restriction scenario passed. The source-pagination scenario initially used the wrong accessible navigation name; after correcting the test locator, its targeted rerun passed, including preserved selection and lost-creation recovery. The 320-pixel invoice screenshot was inspected; overflow and axe checks passed. Real staff MFA/database billing verification and the remaining source variants are still required.
- September 17 lint, TypeScript, all 36 unit tests, production build/standalone packaging and formatting checks passed across the recorded runs. The preserved generated workshop image is absent from the standalone public directory.
- The combined check passed lint, TypeScript and all 36 unit tests, then stopped before compilation on an EPERM while cleaning the old OneDrive build output. Its generated files were preserved in the OS temporary directory; remaining empty generated directories were renamed within the frontend. No application source was removed. The subsequent clean build and standalone preparation succeeded. The deletion attempt was rejected by automatic approval review; the successful recovery used a reversible directory rename.
- Production Edge regression batch: 31 passed, one newly added catalogue test failed, and the real-API journey was skipped because its isolated harness was not running. The catalogue fixture omitted the required envelope `message`; the application correctly rejected it. After correcting that fixture, the targeted production rerun passed in 3.2 seconds. This is 32 verified browser scenarios across runs, not a single clean 32-test batch. Coverage includes all four inventory scenarios, all three billing scenarios, seven public scenarios (including 1024×600 landscape and catalogue failure-to-populated recovery), account/authentication boundaries, quotation/work-order and slot flows. Production homepage and mobile invoice screenshots were inspected. The final added test passed targeted lint/formatting, and TypeScript passed with it included.

## September 17 invoice filters and vehicle sales increment

- Added invoice customer-reference and administrator branch filters with UUID validation, error focus, cursor reset, clear controls and no customer reference in the page URL. The vehicle invoice source scenario verifies eligible linked-customer purchases and a source-only creation request.
- Added staff vehicle purchase list/detail, exact-kobo price negotiation, allowed lifecycle transitions, customer-visible history notes and cancellation review. Nullable legacy recipient/odometer values remain readable in both staff and customer views. The staff projection additionally validates the handover version, phone and cancellation date.
- Added basic handover creation for paid purchases and reviewed READY/COMPLETED/CANCELLED transitions. Creation records recipient, phone, odometer and keys with no fabricated defaults; optional signed evidence is not yet supported by the form. Mutation payloads distinguish handover version from purchase version, and unknown creation is not resent. The backend cancellation recovery gap is disclosed and recorded in release blockers.
- Development browser batch: nine passed, one failed only at the console assertion after completing all handover interactions. It captured Console Ninja's injected loopback WebSocket being blocked by `connect-src 'self'`; the dev server also identified Console Ninja. The security policy and assertion were retained.
- Fresh production build and standalone packaging passed. All ten affected Edge scenarios then passed in one production batch (27.9 seconds): five invoice and five vehicle-sales/handovers tests, including the unchanged console assertion. This is fixture-backed UI evidence, not real staff database/MFA authorization or payment-provider verification.
- Lint, TypeScript, all 36 unit tests, formatting and Git whitespace checks passed for this increment. Production handover review at 320×740 and completed record at 1280×900 were visually inspected; mobile page overflow and axe WCAG A/AA checks passed. Screenshots are in the OS temporary directory as `allied-handover-review-mobile.png` and `allied-handover-completed-desktop.png`. The generated workshop image remains excluded from standalone public assets.

## September 17 signed handover evidence increment

- Added optional PDF preparation/upload, reviewed ticket attachment and explicit private handover access. Runtime `ASSET_STORAGE_HOSTS` supplies approved bare HTTPS hosts to the administrator UI and CSP; empty configuration disables transfer controls. File MIME/size, SHA-256, signed headers, expiry and separate storage origin are checked before upload. Storage PUTs omit application credentials. Pending transfers and in-memory file/link state are discarded on account invalidation.
- Three configured-storage development browser fixtures passed: exact PDF bytes/hash/headers and ticket-only attachment with expiring access; failed upload and unsafe/missing download recovery; cancellation while upload instructions are pending. The fixture storage host is intercepted and never receives real documents. Real storage CORS, backend verification and downloaded content remain unverified.
- Initial upload success testing found a native file picker expanding the fieldset to 324 pixels on a 320-pixel page. Scoped fieldset/file-input constraints corrected the overflow; the rerun passed, including axe checks and an inspected mobile screenshot (`allied-handover-upload-mobile.png` in the OS temporary directory).
- The unit suite now passes 49 tests, including MIME/size limits, approved storage host matching, same-origin transfer rejection and rejection of expired instructions/unsafe headers before file transfer. Lint, TypeScript, fresh production build/standalone packaging and formatting passed. The formatting gate initially identified only the newly edited browser test; that file was formatted and the gate passed.
- Production Edge with the isolated `storage.invalid` allowlist: all eight configured vehicle-sales scenarios passed in 31 seconds; the default-storage scenario was deliberately skipped. This includes the existing negotiation/lifecycle/handover console checks and the three new intercepted upload/access scenarios. The same standalone artifact was restarted with an empty allowlist for the full default suite; those results are recorded separately below.
- Full default production browser batch: 40 passed in 1.6 minutes, with four explicit skips (three configured-storage cases already passed separately, and the real loopback API journey whose isolated harness was not running). Coverage includes public widths 320/768/1024/1440, authentication/account isolation and private cache headers, administrator pickers, inventory, billing, quotations/work orders, slots, vehicle sales and disabled document controls. Together the two production configurations verified 43 distinct fixture scenarios; this is not a real API or storage deployment certification. The server was left on loopback with the empty storage allowlist. Final generated-ledger formatting and whitespace checks passed; the generated workshop image remains excluded from standalone public assets.

## September 17 vehicle catalogue increment

- Implemented eight stock/listing operations: stock list/detail/create/update and listing create/content/price/publication. Staff creation obtains the assigned branch from the existing profile endpoint; administrator branch selection is explicit. Optional stock specifications, VIN, exact integer acquisition cost and Lagos acquisition time are supported. Stock creation does not publish a listing; listing creation starts a draft.
- Five development Edge scenarios passed in 41 seconds: scoped stock creation and versioned edit; draft/content/one-kobo price/publication; preserved stock/listing drafts after concurrent server updates; failed list, administrator filter and cursor-reset recovery; lost stock/listing creation reconciliation. The mobile form passed overflow and axe checks and its screenshot was inspected. These are intercepted fixtures; real staff MFA, branch authorization and database transitions remain unverified.
- TypeScript and lint passed after correcting an uninitialized React ref type and overly widened empty enum defaults. The complete frontend gate passed lint, TypeScript, all 49 unit tests, production build, standalone preparation and formatting. The backend's accepted-but-ignored staff filters are documented in release blockers; no backend contract was silently changed.
- The affected production Edge batch passed 17 scenarios, skipped three configured-storage scenarios and timed out in one screenshot helper. Its dynamic open-panel locator changed indices as panels closed; the helper now closes the first open panel repeatedly. The affected listing scenario then passed in 3.6 seconds, including reserved-state restrictions. This verifies 18 distinct account, vehicle-sale and vehicle-stock scenarios across runs, not a single clean 18-test batch. Production desktop listing and 320-pixel stock-form screenshots were inspected. Storage scenarios remain covered by the separately recorded configured run; these fixtures do not prove real staff authorization or storage integration.

## September 17 vehicle evidence increment

- Added approved raster photo attachment, private document attachment with optional Lagos dates, versioned document assessment and explicit temporary download access. Stock details show attached photo metadata, decimal-string byte sizes and the latest public condition summary. Sold/archived listing content controls now match the backend restriction. Missing attachment edit/delete, private photo preview, condition-report history and PDF download contracts are documented.
- Full frontend check passed lint, TypeScript, all 49 unit tests, production build/standalone preparation and formatting. A development stock navigation timed out during cold compilation; four other stock scenarios passed. Document assessment passed in development. A subsequent configured upload run reached the unrelated development server on port 3000 without storage enabled, correctly finding disabled upload controls; a page-title test also had a case-sensitive expectation. The title assertion was corrected and production validation moved to a separate local port.
- Production Edge at `http://localhost:3001`, with `ASSET_STORAGE_HOSTS=storage.invalid` and fully intercepted API/storage: all eight vehicle-stock scenarios passed in 35.2 seconds, including the three new document/photo workflows and the previously timed-out stock creation. One additional uncertain-document scenario passed separately in 3.3 seconds. This is nine distinct verified scenarios across runs. Upload assertions cover exact PNG/PDF bytes, checksum and ticket-only attachment; document assertions cover pending state, dates, private access, independent versions, concurrent drafts, saved-response loss reconciliation and unresolved no-replay locks.
- The 320-pixel document form passed page-overflow and axe WCAG A/AA checks. Mobile review and 1280-pixel desktop document screenshots were inspected; page identity, meaningful rendering, successful interactions and absence of page exceptions were checked in the document journey. Browser plugin not available; regular Playwright used installed Edge. The other development server was left running. These fixtures do not prove real storage CORS, content verification, staff MFA or branch authorization.

## September 17 inspections and condition reports

- Added paged staff inspection management and status review, complete Lagos schedules, administrator assignee selection, cancellation reasons and preserved concurrent drafts. Condition reports support public review, nullable readings, typed findings and optional completed-inspection linking through paged choices. Report PDFs remain unavailable because no authorized retrieval route exists. Customer/public parsers now accept unrecorded odometers.
- Development Edge: three inspection scenarios passed in 24.7 seconds (staff confirmation/completion, administrator filtering/assignment/concurrent update, cancellation after a lost response). Two condition-report scenarios passed in 33.3 seconds (public typed findings with a paged eligible inspection, and unconfirmed older creation remaining locked). The 320-pixel forms passed overflow and axe A/AA checks; mobile inspection review and desktop condition-summary screenshots were inspected. These use intercepted fixtures, not real staff MFA or database authorization.
- Complete frontend gate passed lint, TypeScript, all 52 unit tests, production build/standalone preparation and formatting. New unit coverage includes finite numeric findings, duplicate names, invalid dates, reading limits and nullable public/customer odometers. Later review-copy/test changes also passed targeted lint and were included in the production compilation.
- Production Edge at `http://localhost:3001`, with intercepted API/storage and the isolated storage allowlist: all 16 scenarios passed in one 51.6-second batch (five inspection/customer-history scenarios and eleven stock/evidence/report scenarios). New coverage includes complete rescheduling intervals, no-show terminal state and customer report rendering with an unrecorded odometer. Existing stock/listing, photo/document attachment, private access and recovery tests passed again with the new report forms mounted.
- Production 320-pixel inspection review and 1280-pixel condition-summary screenshots were inspected after the assignee/linked-inspection review details were added. Page identity, meaningful content, no page exceptions, interaction results, responsive overflow and axe checks passed within the tested journeys. Browser plugin not available; regular Playwright used installed Edge. The isolated test server was stopped after validation. No real API, storage upload, customer notification or production record was used.

## Runtime limitations

- Turbopack development compilation crashed with an internal Rust aggregation panic. Webpack development mode and production build succeeded. Production runtime checks passed for the 11 covered journeys; later edits still require affected checks.
- Playwright uses the installed Edge browser on Windows. The bundled Chromium version was unavailable; no browser installation success is claimed.
- Normal local browsing produced extension-injected hydration attributes and Console Ninja socket errors. Browser evidence uses isolated contexts, without relying on the user's extended browser profile.
- API browser fixtures are synthetic and intercepted. They do not prove real cookie forwarding, PostgreSQL transactions, payment provider integration, delivery or uploads. No real card was charged or customer notification sent.

## September 17 product compatibility and image increment

- Added reviewed CRUD for compatibility and hosted image records from the administrator parts list. Browser fixtures cover full nullable year-bound payloads, invalid/reversed years, malformed/insecure/credential-bearing URLs, primary replacement and deletion, CSRF, child-only stale changes, failed preflight recovery, contradictory success, unknown-result locks across editor reselection, staff denial and account invalidation. No product-detail GET or upload route was invented.
- Initial development testing found two return-focus failures. Avoiding unnecessary cancellation refetch/disabled openers and preserving the original dialog opener across React Strict Mode effect replay fixed them; the affected compatibility/basic-edit/mobile tests passed together. Visual inspection then found clipped long review values at 320 pixels; scoped grid wrapping and an internal review-region overflow assertion fixed that. A malformed-URL unit case exposed a throwing refinement; it now returns a field error, and the browser image-validation scenario passed again.
- All 59 unit tests in seven files pass. Full lint passed, followed by focused lint after the final validation fix; TypeScript and full formatting checks passed. A fresh production build and standalone packaging passed outside OneDrive; all 244 runtime/configuration/public/packaging files matched the workspace by SHA-256 before production testing. The development cache-header assertion returned Next's development `no-cache, must-revalidate`; the same unchanged test passed on the production server with its required private `no-store` header.
- Final production Edge batch: all 51 tests passed in 1.6 minutes (12 product metadata/basic-save tests, seven account regressions, five invoice scenarios, ten operational queue scenarios and 17 staff administration scenarios). Mobile editor and review at 320?740 passed axe WCAG A/AA and internal/page overflow checks; the final mobile review and 1280?900 desktop captures were inspected. Screenshots are in the OS temporary directory as `allied-product-extras-mobile.png`, `allied-product-review-mobile.png` and `allied-product-extras-desktop.png`. The temporary standalone server was stopped afterward; the existing development server was left running.
- These are intercepted UI and local build checks. Real privileged MFA/API authorization, database concurrency, genuine image hosting and approved media-host deployment remain unverified. Backend revision and merged year-range validation gaps are recorded in the release blockers. No backend contract was changed.

## September 17 promotions increment

- Added administrator list/create/detail/versioned edit and customer cart code previews. Fixtures exercise exact kobo/basis-point payloads, nullable limits/code, cross-field errors, discount-type switching, stale draft reload, unknown/contradictory write locks, permission denial, preview invalidation and failed/late previews. Checkout receives only the promotion code alongside its existing branch/fulfilment fields, never an estimated discount.
- All 63 unit tests in eight files passed, including new precision/range/limit tests and valid sub-minute promotion windows. Date controls and review values preserve seconds/milliseconds and Lagos time. Full lint passed before the final precision/layout changes; focused lint and fresh TypeScript/formatting gates passed after those changes.
- Initial development browser run: 12 passed and one selector failure where Next matched the development-tools button; exact Next selection corrected the test and its rerun passed. The later precision change exposed native date-input overflow at 320 pixels. Reusing the constrained record form grid fixed it; the mobile test passed with no page/internal review overflow or axe WCAG A/AA violations. Versioned-edit precision and account invalidation tests also passed. Current mobile form and review captures were visually inspected.
- Final production build and standalone packaging passed outside OneDrive. All 254 runtime/configuration/public/packaging files matched the workspace by SHA-256. All 33 selected Edge tests passed in 1.1 minutes: 14 promotion scenarios, seven account/checkout regressions and 12 product-editor regressions. The promotion mobile form and review passed page/internal overflow and axe checks; the final scenario recorded no JavaScript exceptions or console errors. Final 320x740 review and 1280x900 desktop captures were inspected (`allied-promotion-mobile.png`, `allied-promotion-review-mobile.png`, `allied-promotion-desktop.png` in the OS temporary directory). The temporary production server was stopped afterward; the existing development server was left running.
- These are intercepted browser checks. Real privileged MFA, row-lock/expectedVersion concurrency, duplicate codes, usage-limit consumption and checkout redemption are still unverified. No actual promotion, order or notification was created or modified.

## September 17 account security increment

- Implemented seven actual identity endpoints for password changes, active sessions and revocation, factor reads/removal and recovery-code replacement. Customer and staff security pages share validated panels. Tests cover partial/malformed reads, cleared credentials, safe field errors, final-factor restrictions, pending factors, lost responses, explicit recovery replacement and private-state invalidation.
- All 69 unit tests in eight files passed. Full lint and TypeScript passed; the final browser test edits passed focused lint. Formatting initially found the generated endpoint JSON; it was formatted and the full formatting gate passed. The inventory still contains 225 operations.
- The initial 16-scenario development run found a real self-broadcast bug: password rotation was followed by this tab receiving its own cross-tab event, losing the success acknowledgement and rotated CSRF token. Random non-account source identifiers now distinguish the sender. The two targeted password/cross-tab scenarios passed after the fix, and a unit regression verifies own-event filtering with external-event compatibility.
- Production build and standalone packaging passed outside OneDrive, with all 262 runtime/configuration/public/packaging files matching the workspace by SHA-256. All 57 selected production Edge scenarios passed in 1.6 minutes: 16 security, seven account, 17 staff administration, 14 promotion and three authentication regressions. The security success test checks two actual browser tabs: the sender retains its password-change acknowledgement while the other tab clears private state. The mobile form and review passed axe WCAG A/AA and overflow checks; the final scenario reported no JavaScript exceptions or console errors. Final 320x740 review and 1280x900 desktop captures were visually inspected (`allied-security-mobile.png`, `allied-security-review-mobile.png`, `allied-security-desktop.png` in the OS temporary directory). The temporary standalone server was stopped; the existing development server was left running.
- These checks use intercepted API fixtures. Real database revocation, MFA authorization, cookie rotation and concurrent credential/factor/recovery replacement remain unverified. Backend behavior was inspected and gaps recorded; no backend contract or real account was changed. MFA enrollment/challenge improvements remain unfinished.

## September 17 MFA enrollment and challenges increment

- Implemented all six MFA enrollment/challenge endpoints with runtime-validated session/options/results, multiple authenticator selection, recovery-code consumption, TOTP setup and browser WebAuthn registration/assertion. Enrollment requires acknowledgement that old recovery codes are replaced. Unknown verification results are locked and never automatically retried; session checks do not unlock a still-pending verification.
- All 75 unit tests in nine files passed, including response-policy boundaries and caller cancellation while response JSON is being decoded. TypeScript, full lint and formatting passed. The endpoint inventory remains 225 operations, now with the six MFA mappings.
- The initial development browser run passed all 16 MFA scenarios. Four targeted follow-up checks passed for browser cancellation, failed bootstrap reassessment, uncertain pending sessions and clearing displayed enrollment codes after account changes. Virtual Edge WebAuthn registration and authentication produce actual browser credentials, but the verification endpoints are intercepted fixtures.
- Mobile inspection found floating support controls obscuring auth content. Authentication screens now use an inline help link; the shared public support widget remains available on public pages. The enrollment title is shorter at narrow widths, and method selection focuses its next relevant input.
- Production compilation, TypeScript, route generation and standalone packaging passed outside OneDrive. All 267 runtime/configuration/public/packaging files matched the workspace by SHA-256. All 69 selected production Edge scenarios passed in 1.9 minutes: 19 MFA, 16 account security, seven account/checkout, 17 staff administration, three authentication and seven public/help cases. Mobile setup and recovery-code states passed axe WCAG A/AA and overflow checks, with no JavaScript exceptions in the visual scenario. Final mobile setup/recovery and desktop recovery captures were inspected (`allied-mfa-setup-mobile.png`, `allied-mfa-codes-mobile.png`, `allied-mfa-codes-desktop.png` in the OS temporary directory). The temporary standalone server was stopped; the existing development server was left running.
- Real isolated TOTP/WebAuthn verification, replay and concurrency behavior, cookie rotation, physical devices and additional browser engines remain unverified. Inspection identified a critical existing-factor enrollment bypass in backend pending-session authorization; the frontend bootstrap restriction is not a substitute for server enforcement. No backend rule or real account was changed.

## September 17 backend MFA authorization correction

- A new disposable PostgreSQL 17 container on loopback port 55433 applied all 30 existing migrations. It was stopped and automatically removed after backend verification, preserving append-only audits until the entire disposable database was removed. Initial regression checks failed for all four account roles: pending sessions could obtain TOTP setup and WebAuthn registration options despite an active factor. No production database, real account or delivery provider was used.
- Backend enforcement now serializes account/session eligibility, validates the middleware-observed CSRF revision and repeats authorization at activation. Factor activation, code replacement, audit and session rotation commit together. Final-factor removal and recovery replacement share the account lock. HTTP success bodies remain unchanged; enrollment denial and stale-session responses are documented in OpenAPI and generated frontend types.
- The first expanded regression run found fixture timestamp violations and an attempted user cleanup blocked by append-only audit rules. Fixture timestamps now satisfy database constraints; fixture records remain until the disposable database is removed. The corrected 17-test focused run passed (16 new cases plus the existing identity flow). The full backend `npm run check` then passed all 168 tests in 45 files, secret scanning, Prisma validation/status, application/script/config TypeScript, OpenAPI, lint and formatting. A later additional older-customer-session case passed with all 17 current MFA security cases; the backend build also passed.
- The initial full gate found pre-existing sensitive example values. The Resend key-shaped value and literal database password were replaced without printing them, and the MFA-key placeholder was made explicit. The worktree secret guard passed. Their historical exposure, validity and any external revocation remain unverified.
- Frontend regenerated types, all 75 unit tests and TypeScript/formatting passed. An enrollment MFA_REQUIRED response now offers an existing-factor verification path for an older session, disables further setup and discards its displayed setup secret. The development-only check timed out because the existing server returned HTTP 500; its log identified EBUSY on `.next/dev/server/next-font-manifest.json` under OneDrive. The outside-OneDrive production build and packaging passed, with all 267 runtime/configuration/public/packaging files matching the workspace. All 40 selected production Edge scenarios passed in 54.6 seconds: 21 MFA, 16 account security and three authentication cases. These include both older-session enrollment denial and hiding a prepared TOTP secret when activation now requires prior MFA. Mobile accessibility/overflow and existing recovery/privacy checks passed. The temporary standalone server was stopped; the existing development server was left untouched with its documented OneDrive EBUSY issue.
- Remaining limits: no real browser-to-API WebAuthn signature journey or physical-device compatibility claim; password-change concurrency/rotation and failed rotation after single-use sign-in verification remain open. Backend enforcement is now implemented and locally verified, superseding the prior critical enrollment blocker.

## September 17 notification inbox and preferences increment

- Added ten customer/staff endpoint mappings and runtime validation for actual notification/preference projections. Backend routes, schemas, service delivery defaults, selectors and database preference-scope constraints were inspected. No backend contract was changed in this increment.
- The initial type check found a callback narrowing issue in preference rendering; using the stable parsed preference list fixed it. The first browser fixture omitted the required response-envelope message and was rejected before session rendering. Correcting that fixture produced a 12/12 pass; the response validator was preserved. A transient OneDrive append lock was resolved by applying the test edit normally, without stopping user processes or deleting caches.
- Screenshot review led to the existing accessible checkbox treatment and a two-column preference layout. Fresh production compilation, TypeScript, route generation and standalone packaging passed outside OneDrive; all 270 runtime/configuration/public/packaging files matched the workspace by SHA-256. Full frontend lint, TypeScript, formatting and all 75 existing unit tests passed; focused test lint passed before the final browser run.
- Final production Edge run at `http://127.0.0.1:3001`: all 40 scenarios passed in 55.4 seconds (17 notifications, 16 account security, seven public/help). Notification coverage includes filters/cursors, CSRF, individual/all reads, all four roles, sparse defaults, explicit marketing consent, missing consent, independent failures, malformed/contradictory responses, uncertain-write locks, forbidden changes and cross-tab account invalidation during a write.
- Mobile 320x740 notification and consent states passed axe WCAG A/AA and page overflow checks. Keyboard dialog initial/return focus passed; the visual scenario reported no page exceptions or console errors. Page identity and meaningful content were checked, with no framework overlay. Final mobile and desktop 1280x900 screenshots were inspected: `allied-notifications-mobile.png`, `allied-notifications-review-mobile.png`, and `allied-notifications-desktop.png` in the OS temporary directory. Browser plugin was unavailable; repository Playwright with installed Edge was used. The temporary standalone server was stopped after testing; the user's existing development server was untouched.
- These are intercepted UI and local build checks. Real API ownership enforcement, database preference concurrency, actual email/SMS delivery, additional browser engines and broader intermediate/landscape notification checks remain unverified. No real message, account preference or production record was changed.

## September 17 review workflows increment

- Replaced manual customer identifiers with completed-record pickers and added all five supported review targets. Added paginated moderation history, administrator-only moderation and public approved feedback. Inspected backend eligibility, selectors, request validation, versioned decisions and notification behavior. No backend contract changed in this increment.
- Initial type checks found rating-refinement inference and heterogeneous source-list inference issues; explicit validated types fixed them. The first 18-case production browser run passed 16 and found duplicate moderation success announcements plus lost dialog-opener focus. A single persistent parent success message and keeping native-modal background openers enabled fixed both; no validation or authorization guard was weakened.
- Full lint, TypeScript, formatting and all 75 unit tests passed. After the final heading/copy changes, focused lint and formatting passed again. Production compilation, TypeScript, route generation and standalone packaging passed outside OneDrive; all 279 runtime/configuration/public/packaging files matched the workspace by SHA-256.
- Final production Edge batch at `http://127.0.0.1:3001`: 43 scenarios passed in 1.5 minutes (19 reviews, 17 notifications, seven public/help). Review coverage includes explicit rating, all five exact target bodies, completed-source rechecks, blocked stale sources, recoverable preflight failure, unknown/contradictory creation, history failures/filters/pagination, rejection reasons, expectedVersion, stale/unknown moderation, staff restrictions, public projection privacy, account invalidation and a simulated approval-to-publication journey.
- Customer 320x740 and administrator 390x844 layouts passed axe WCAG A/AA and page overflow checks. Dialog initial/return focus passed; the customer visual scenario had no JavaScript exceptions or console errors. Final customer, moderation, public desktop and consent screenshots were inspected. A clipped dialog screenshot was traced to capture during smooth page scrolling: DOM bounds were valid. Freezing that scroll before capture produced a complete image; the focused scenario passed with explicit dialog/title viewport bounds. This was a capture correction, not a claimed application layout repair.
- Screenshots remain in the OS temporary directory: `allied-reviews-mobile.png`, `allied-reviews-desktop.png`, `allied-review-dialog-mobile.png`, `allied-review-moderation-mobile.png`, and `allied-public-reviews-desktop.png`. Browser plugin was unavailable, so repository Playwright and installed Edge were used. These are synthetic intercepted workflows: no real review, public publication or customer notification occurred. Real API ownership, duplicate-source behavior, concurrency, delivery, additional browsers and server-rendered public-review SEO remain open.

## Outstanding verification

Additional real isolated API journeys, especially staff MFA and branch authorization; complete account recovery/security and remaining staff workflows; deployment-level HTTPS/cookies/CSP/private documents; saved items, inspections, vehicle purchases and invoice variants; upload failures; manual payments/refunds; remaining endpoint coverage; broader intermediate/landscape/keyboard checks; measured performance; final acceptance and fresh gates after remaining implementation. The earlier customer loopback journey and selected production cache checks are already verified within their stated scope.

## September 17 customer care increment

- Implemented 25 public/customer/staff enquiry and complaint endpoints. The flow under test is contact or customer-care queue -> select source/branch and review creation -> dedicated conversation -> paginated history/reply; staff additionally reviews assignment, lifecycle and complaint-priority changes. All payloads and records in these browser tests are synthetic and intercepted.
- Full frontend lint, TypeScript and all 75 unit tests in nine files passed. Focused lint/type checks passed after later semantic, responsive and test changes. Fresh production builds and standalone preparation passed outside OneDrive; all 290 runtime/configuration/public/packaging files match the workspace by SHA-256. Full formatting passed, with changed documentation formatted again after the evidence update.
- Production Edge at `http://127.0.0.1:3001`: the cross-domain batch passed all 53 scenarios in 2.6 minutes (33 initial support scenarios, six staff booking scenarios, two appointment-slot scenarios, five inspections and seven public scenarios). After the final long-text wrapping and conversation-control changes, the affected batch passed all 42 scenarios in 1.7 minutes: 35 support scenarios and seven public regressions. There are 55 distinct passing scenarios across these batches.
- Initial fixture responses omitted required envelope metadata; this was corrected without changing the API client. Three error assertions also matched Next's empty route announcer; scoping to the main landmark fixed the tests. Full axe checks found shared mobile navigation and quick-help controls outside navigation landmarks. These now use labelled nav elements. Visual inspection found floating help covering a conversation date at 320 pixels; customer-care pages omit the duplicate floating controls. Long unbroken subjects/contact values now wrap and have a dedicated narrow-screen test.
- Page identity, meaningful content, no framework overlay, console/page-error health, dialogue bounds, keyboard dismissal/return focus, preserved drafts, page/internal overflow and automated axe checks passed on the tested 320-, 390- and 1280-pixel support views. The public product enquiry form/review also passed narrow-screen axe and overflow checks. Mobile conversation, customer/staff reply reviews, desktop staff detail and public submission-review captures were visually inspected. Evidence is in the OS temporary directory: `allied-support-page-320.png`, `allied-support-320.png`, `allied-support-390.png`, `allied-support-page-1280.png` and `allied-support-public-320.png`.
- Browser plugin not available; regular Playwright used installed Edge. The existing development server was left untouched. No genuine enquiry, complaint, reply, notification, email or production mutation was sent. Real ownership/branch authorization, rate limits, concurrent status/message writes and outbox delivery still require isolated API verification. No backend contract was changed in this increment.

## September 17 cart maintenance and expiry audit increment

- Added reviewed DELETE `/customers/cart` with exact quantities/kobo, fresh parsed-state comparison and no write after a changed or unreadable preflight. Unknown clear/item mutations pause further edits and checkout even after refresh; original checkout idempotency recovery remains intact. Requests abort on account-view unmount and failed cart reads hide prior contents. Administrator overview now explains bulk expiry unavailability and links to real order/purchase screens.
- Audited both manual expiry services against their system counterparts. Source evidence shows manual paths omit payment-in-flight/review exclusions; vehicle manual expiry also lacks a deadline recheck under the transition lock. These two endpoint mappings explicitly explain why the browser does not call them. No manual expiry POST, backend policy change or genuine cart mutation was performed. Real concurrent settlement/expiry and cart compare-and-delete guarantees remain unverified.
- Full frontend lint, TypeScript, all 75 unit tests in nine files, production build/standalone preparation and formatting passed. The final standalone copy outside OneDrive matched all 290 workspace runtime/configuration/public/packaging files by SHA-256. The 225-operation inventory now has an explicit mapping/reason for every non-webhook operation; that is coverage accounting, not proof of full workflow or real API completion.
- Production Edge at `http://127.0.0.1:3001`: all 44 scenarios passed in 1.4 minutes (13 new cart-maintenance/expiry-availability scenarios, seven account regressions, ten operations scenarios and 14 promotion scenarios). Initial visual inspection found review headings inherited inconsistent sizes and description spacing was cramped. Shared native-review heading/paragraph styles were corrected. A fresh build then passed all eight affected cart/promotion/support responsive scenarios in 51.7 seconds.
- Flow under test: customer cart -> Clear cart -> review exact contents and consequences -> confirm -> fresh empty state; cancellation restores focus, changed contents abort deletion, and unknown results prevent further writes. Page identity, meaningful content, no framework overlay, console/page-error health, axe, narrow-screen overflow and keyboard cancellation passed on the tested views. Final 320- and 1280-pixel cart review captures were inspected (`allied-cart-clear-320.png`, `allied-cart-clear-1280.png` in the OS temporary directory); support regressions also cover 390 pixels. Browser plugin not available; regular Playwright used installed Edge. These remain intercepted UI checks, not real stock, cart, payment or database tests.

## September 17 vehicle payment-request increment

- Added reservation, partial, balance and full vehicle payment-request purposes. The browser submits only target type, owned purchase ID and purpose; the backend calculates kobo. Complete paginated payment history, fresh purchase/version/price/deadline checks and existing-attempt checks precede a new request. Unknown creation retains its exact idempotency key/body, including after rejected replay. Payment detail validates the requested identity and links back to purchase progress. No backend financial rule or reservation consent contract changed.
- Full frontend lint, TypeScript and all 75 unit tests in nine files passed. The initial production build and standalone preparation passed outside OneDrive. Production Edge passed 47 scenarios in 2.0 minutes: 26 new vehicle-payment cases plus account, manual-payment and vehicle-sales regressions; seven storage cases were skipped without an approved storage host. With the synthetic storage host configured, the first run passed six cases and found a hardcoded localhost origin in the handover test fixture. Using the actual test-page origin fixed the fixture without weakening upload validation or CORS; all seven configured cases then passed.
- The vehicle suite covers all four exact request bodies, CSRF/idempotency, later-page unresolved records, complete-history failures, stale preflight, unknown/rejected recovery, contradictory success responses, terminal states, missing amounts, prior settlement, deadline expiry and account invalidation. Mobile 320 and desktop 1280 views passed axe, overflow, dialog focus and console/page-error checks. Their review captures were visually inspected (`allied-vehicle-payment-320.png`, `allied-vehicle-payment-1280.png` in the OS temporary directory).
- Newer shared-workspace homepage edits were preserved and included in a fresh build. Public checks found two red-on-navy small labels at 3.97:1 contrast; a brighter red token preserves the red treatment. Browser inspection also found five implicit grid tracks with inconsistent child widths after the layout changed to one column. Removing obsolete child spans preserves the single-column layout with aligned widths. The final production build and standalone preparation passed; all 293 audited runtime/configuration/public/packaging files matched the workspace by SHA-256. All nine affected production Edge checks passed in 30.7 seconds: seven public/help cases at widths including 320, 768, 1024 and 1440 pixels, and both vehicle-payment responsive cases. Across the vehicle increment, 61 distinct browser scenarios passed in the stated batches. Full formatting passed, followed by focused lint/format checks on the final homepage and fixture changes. The final grid inspection confirmed one track with equal child widths.
- These are synthetic intercepted browser flows, not real provider charges or settlement. Backend creation locks only the request key, so independent keys can race. Mutable source validation precedes idempotency lookup, so recovery can fail after source changes; both issues are documented for coordinated backend work. Approved reservation terms/version, genuine provider completion and broader release checks remain outstanding. The endpoint inventory still accounts for 225 operations; this is not a full-release claim.

- Final mobile homepage and both payment-review screenshots were inspected; homepage automation also captured each tested width. The temporary standalone server was stopped after verification, leaving the existing development server untouched. No genuine charge, upload, account mutation, customer notification, deployment or production operation occurred.

## September 17 public SEO and indexing increment

- Added configured canonical identity, page-specific public Open Graph/Twitter summaries, dynamic robots, complete public-catalogue sitemap scanning and factual homepage AutoRepair JSON-LD with safe serialization/CSP nonce. Public detail metadata now preserves not-found interrupts and rejects malformed/request-mismatched identities. Authentication/private pages remain noindex and no-store; query variants retain clean canonicals with noindex. No backend contract changed and no production domain was invented.
- Inspected current route rendering, public selectors and pagination limits. Services/products/vehicles already had detail SSR; this increment verifies and strengthens that behavior rather than claiming a new rendering migration. Backend public product queries require active product/category; vehicle queries require available listings and active branch. Sitemap consumes only public endpoints, omits private credentials, and fails rather than reporting partial catalogue success. Public index lists and approved reviews remain client-fetched and are still work items.
- Full lint, TypeScript and all 94 unit tests in 11 files passed. The 19 new SEO/sitemap unit cases cover invalid origins, disabled indexing, filtered canonical policy, private-route classification, factual/escaped JSON-LD, all-page scanning, malformed data, repeated IDs/cursors, backend failures and cancellation. Focused unit tests passed again after a test-only typing improvement. Production build and standalone packaging passed outside OneDrive; SHA-256 comparison matched all 297 audited runtime/configuration/public/packaging files.
- First production Edge batch at http://127.0.0.1:3001 passed all 39 scenarios in 27.8 seconds: 32 dedicated SEO cases and seven public/help regressions. The dedicated suite owns a synthetic loopback API on 5011; the app uses a synthetic .example canonical identity. It verifies seven public routes, clean query canonicals/noindex, ten private/account routes, all three detail types without JavaScript, one backend read per render, browser credential isolation, crawler 404/noindex, safe 500 errors for unavailable/wrong-identity data, later-page sitemap URLs, incomplete-scan 503 and nonce-bearing business schema.
- Missing-origin deployment checks initially found only an assertion that expected one trailing robots newline while Next emits two. The assertion now compares trimmed directive content. The corrected second batch passed all 12 scenarios in 21.1 seconds: two missing-origin checks, seven account cases and three authentication cases. Even SITE_INDEXING=enabled cannot index public pages without a valid origin; robots blocks all and sitemap returns 404. There are 51 distinct passing scenarios across the two batches.
- Target flow: public part URL -> meaningful server-rendered detail -> Ask about compatibility -> contact page. At 320 and 1280 pixels, page identity/title, rendered heading, JavaScript console health, axe, page overflow and navigation passed. Screenshots allied-seo-part-320.png and allied-seo-part-1280.png in the OS temporary directory were inspected, showing readable exact kobo and preserved responsive layout without a framework overlay. Existing public scenarios cover 768/1024/1440 pixels, help dismissal/focus and catalogue failure recovery. Browser plugin not available; regular Playwright with installed Edge was used.
- Expected server logs for the deliberate 503 and wrong-identity fixtures contain only the safe public error, while client HTML omits fixture-private messages. The temporary standalone server and loopback fixture were stopped; the existing user development server was untouched. No genuine charge, message, record mutation, upload, deployment or search-engine submission occurred.
- Remaining limits: actual production origin and deployment access restrictions, real crawler/indexing behavior, SSR for public list/review content, approved share photography, sitemap performance at scale and concurrent catalogue changes, broader browser/device coverage and measured performance. HTTP status assertions for missing/failing details use a non-streaming crawler user agent; they do not claim all streamed user agents have identical status timing. The endpoint inventory remains 225 operations and records the added sitemap/detail consumers.

## September 17 public discovery rendering increment

- Added validated server seeds for services, parts, vehicles, approved reviews and all three homepage catalogue previews. Metadata and page reads use the same request-scoped cache. Successful seeds skip immediate duplicate hydration reads; filters, cursor changes, explicit refresh, reconnect and visibility still fetch current data. Failed initial collection reads are noindex/error rather than empty. Failed subsequent reads hide stale public cards, including withdrawn review content. No backend contract changed.
- The public review selector was inspected: it filters APPROVED and selects only public fields. Runtime schemas strip additional fields before HTML and hydration serialization. The isolated API fixture deliberately includes private extra fields and script-shaped review text; public output drops the private fields and renders the comment as inert text. No real review or business claim was introduced.
- The initial TypeScript run found an incorrectly placed metadata insertion; it was corrected. Full lint, TypeScript and 101 initial unit cases then passed. An additional resource-hook regression reproduced public content remaining in loading state after session invalidation; public paths now reload while private resources retain their discard boundary. All 102 unit tests in 12 files passed after the fix, including eight new hook cases for initial seeds, filtering/return, default behavior, recovery, reconnect/visibility, private invalidation and abort.
- The first expanded production browser run passed 45 of 48 cases. It found streamed homepage previews hidden when JavaScript was disabled and service cards skipping from h1 to h3. Homepage public reads now run concurrently before rendering the page content, and standalone service cards use h2 while homepage cards retain h3. This preserves readable no-JavaScript first-page content; the existing six-second upstream timeout remains a performance consideration.
- A subsequent 90-case production batch passed 89 in 3.0 minutes. Account/authentication, public/help and review regressions passed, as did public-only SSR, no duplicate initial reads, filter/page/reset transitions, empty/malformed/outage distinctions, partial homepage failures, review refresh recovery and responsive checks. The remaining case showed that public pages lacked the cross-tab listener used by dashboard/MFA screens. The public header now delegates validated BroadcastChannel messages to the existing session-generation invalidation function. No account data is broadcast.
- Visual review of the 320-pixel reviews capture found floating help covering part of the Next button despite passing axe and overflow checks. Public-review pagination now reserves horizontal space for the help controls and places its page label above the two buttons at narrow widths. A bounding-box assertion supplements the existing interaction/accessibility checks. All 299 audited runtime/configuration/public/packaging files matched the standalone source copy before the final rerun.
- Target flow: public route -> server-rendered published first page -> browser filter -> next/previous -> reset; plus published review -> failed refresh -> hidden stale content -> successful refresh, and cross-tab account change -> fresh public list. Without JavaScript, first-page content and ordinary detail links remain readable; filters/further paging/refresh require JavaScript and the page explains that limitation. Filter state is not yet a shareable navigation URL.

- Final affected production Edge validation passed all 61 SEO/public cases in 1.5 minutes, followed by two public-review/moderation regressions in 11.7 seconds. Together with the earlier account/authentication and review batch, all 90 distinct scenarios have passing results within this increment; this is not a claim that the entire repository browser suite was rerun on the final build. The final 320-pixel review screenshot was inspected and confirms both pagination buttons remain unobstructed by floating support. The screenshot uses synthetic test feedback only.
- Final production build/standalone packaging, TypeScript and full formatting passed after the header and pagination fixes; focused lint passed and the full 102-unit-test result remains applicable. The temporary standalone server was stopped after verification. No real account mutation, payment, customer notification, upload, production deployment or search submission occurred. Component-owned public mutation results still require their own account-change reset audit, documented in release blockers.

## September 17 public detail account-isolation increment

- Inspected the three public action components, API session-generation checks, cross-tab header bridge, booking idempotency policy and authenticated vehicle routes. The resource hook already reset fetched records, but component-owned booking results, product/vehicle result messages, booking retry keys and inspection notes survived a session change.
- Added shared mutation lifetime/reset handling for booking, cart quantity, favourite, saved vehicle, vehicle enquiry and inspection. Pending calls receive an AbortSignal; success, failure and finally handlers cannot restore old results or unlock newer work. Local fields/consent and booking attempts reset. The visible account-recovery notice explicitly says a sent request may still have completed. No backend contract, authorization rule or financial policy changed.
- A regression run against the previous production build failed after the cross-tab event. Its DOM snapshot still contained Booking request received, the deposit/hold and Review booking & deposit link. Four new lifecycle unit cases cover synchronous duplicate starts, reset/abort, old-finalizer/new-request races and unmount cleanup. Full lint, TypeScript and all 106 unit tests in 13 files passed.
- Target flow: public detail -> complete or hold a customer action -> cross-tab account change -> cleared fields/results and account recovery -> fresh user action. Browser validation also covers 401 invalidation, same-session booking replay and new-session key replacement, at 320 and 1280 pixels. Browser plugin not available; repository Playwright/Edge uses synthetic public SSR on loopback 5011 and intercepted browser mutations. Production browser results and subsequent presentation fixes are recorded below.

- First updated production run passed all 26 functional cases (16 new mutation/session scenarios plus ten account/authentication regressions) but failed both responsive cases because the recovery link had 4.37:1 contrast on the notice background. The link now inherits the notice's dark text and is explicitly underlined. The booking notice was also moved outside the conditional service-data body so upstream refresh failure cannot hide account recovery; a dedicated regression covers that combination. The first production build and full formatting gate passed, and all 301 audited runtime/configuration/public/packaging files matched the source snapshot. The affected rerun is recorded below.

- An exploratory ESLint cyclomatic-complexity scan at threshold 20 found 36 warnings across 33 application/library files, saved outside the repository as allied-complexity-baseline.json. This is diagnostic evidence only: cyclomatic complexity is not the requested cognitive-complexity gate, and no rule was suppressed or weakened. The cognitive-complexity requirement remains open.

- After the contrast correction, all 19 dedicated public-detail browser cases passed in 1.1 minutes. The recovery-during-outage case passed, and both viewport cases passed axe, overflow, keyboard focus and JavaScript console checks. The earlier ten account/authentication regressions also passed, giving 29 distinct verified browser scenarios across these bounded runs. Mobile screenshot inspection then found the global unlayered anchor rule suppressed Tailwind's underline utility. A narrowly scoped notice-link style now preserves the underline; the responsive test checks its computed style explicitly. Final presentation verification is recorded below.

- The final production rebuild and standalone packaging passed. Both responsive reruns passed in 34.5 seconds across all three detail surfaces at 320 and 1280 pixels, including computed underline, axe, page overflow, keyboard focus, page identity and empty JavaScript error/console-error lists. Final mobile service and desktop part screenshots were inspected in the OS temporary directory (`allied-account-change-0-320.png` and `allied-account-change-1-1280.png`). Screenshots contain synthetic fixtures only. All 301 audited runtime/configuration/public/packaging files matched the standalone source copy. Full formatting and whitespace checks passed; full lint/TypeScript and 106 units passed earlier, followed by focused lint/TypeScript and successful production builds for the presentation edits.
- Manual screenshot review also identified a separate existing floating-support layout limitation: at some narrow viewport scroll positions, the help control overlaps the lower-right portion of the workshop branch selector, and floating controls sit over inspection text. Automated axe/overflow checks do not establish freedom from such overlap. Record-specific fields/results clear correctly, but this mobile layout issue remains in release blockers.
- The owned standalone server was stopped; the public-read fixture closes its own listener after each suite. The existing user development server was not altered. No real customer mutation, notification, booking, financial action, upload or deployment occurred. Browser results prove UI handling of isolated responses and invalidation, not real cross-account backend authorization or concurrent server settlement.

## September 17: owner protection, grouped navigation and public support

- Read both implementation briefs and the supplied extensionless WebP dashboard reference. The user explicitly confirmed Quicksand throughout, selected alliedautotechltd@gmail.com for sole ownership and approved branch operational STAFF access with finance/team management denied. A read-only local database check found no selected account; no real user was created, verified or promoted.
- Added forward database owner protections and replaced password-creating bootstrap with explicit existing-account promotion. Three dedicated fresh-database scenarios passed after the final protection preflight. Full database-enabled backend regression passed 169 tests across 45 files, with the dedicated fresh-owner suite separately gated. Backend types, script types, lint, formatting and build passed.
- Grouped the shared sidebar into 7 customer, 6 STAFF and 8 administrator groups, preserving routes and filtering each child. Added permitted-page search, accessible disclosures, current-group expansion and mobile Escape/focus recovery. Ten production Edge cases passed across all four roles and 360, 390, 768, 1024, 1440 and 1655 widths. The search retyping-after-Escape defect found by these tests was fixed.
- Inspected reference and current 1655 × 1133 / 390px screenshots. Sidebar/header are integrated; existing overview content is deliberately still shown until authorized aggregate contracts are implemented. Five summary cards, chart/list and table/list are not yet complete. Do not claim full reference fidelity.
- Added approved homepage feedback through existing public seed/review components, anonymous-support account-change resets and pending/unknown locks, and a non-overlapping narrow-screen help/WhatsApp bar. A 123-pass browser run verified public privacy, SEO and homepage-review behavior before three old sidebar contrast failures stopped support checks. White inverse sidebar/footer branding fixes that issue. The final combined 147-case run is in progress; final totals belong in the dashboard handoff.
- Role-dependent navigation does not establish API authorization. Existing STAFF finance endpoints, secure verified-account promotions/invitations and general role-scoped analytics remain open, as documented in dashboard-integration-handoff.md. No financial invariant or unapplied business policy was weakened.

- Final shell/public/support production Edge run passed all 147 scenarios in 5.7 minutes, including all new contact-reset races and uncertain-submission locks. Finance entry-point enforcement then passed 27 distinct browser scenarios across a 26-pass run and a corrected administrator-fixture rerun. One optional evidence-storage allowlist case was skipped. No runtime defect remained from that bounded finance run.
- Five new disposable backend finance-boundary scenarios passed. The broader post-finance run passed 173 tests with one synthetic singleton-owner MFA fixture count failure on database reuse; isolating its named synthetic factors fixed the fixture, and all 17 affected MFA cases passed on rerun. The original owner-protection suite passed separately on a fresh database. No assertion or production MFA constraint was weakened.
- Final backend type/script checks, lint, formatting, OpenAPI validation and build passed. All 10 retained financial staff-path operations now document ADMIN/SUPER_ADMIN access. Regenerated types and the 225-operation inventory match those annotations. Final source audit compared 302 runtime/configuration/public/packaging files; the only difference was regenerated type-only declarations, which were copied to the external source snapshot without changing browser runtime.

## September 18: existing-account Team & Access

The old unauthenticated account-creating invitation flow is replaced with verified-customer promotion and authenticated, recipient-bound STAFF-to-ADMIN invitation acceptance. Existing session/MFA/CSRF, current-password verification, encrypted outbox and audit infrastructure are reused. Generic ADMIN assignment is removed; the owner can demote administrators to branch staff. A forward migration binds recipients, preserves legacy history and revokes unused old links.

All 32 migrations replayed on a disposable database. Focused API/policy tests passed 15 cases; the expanded full suite passed 187 and failed only the outdated endpoint-count assertion. The corrected 229-operation handbook test passed separately; all 12 team cases passed, including concurrency, revocation and lost assurance. Three separately gated owner tests were skipped in this run and have previous dedicated fresh-database evidence.

Frontend production build, types and lint passed; one-worker units passed 106/106 after an initial parallel run could not start two workers. Eighteen distinct team browser scenarios passed, followed by the final combined 31-case team/navigation run recorded in dashboard-integration-handoff.md. Visual inspection caught duplicate active sidebar links on the invitation route; the longest matching destination now wins. Browser plugin unavailable; repository Playwright/Edge used loopback production output and isolated API fixtures. Real email delivery, real account provisioning, overview analytics/reference composition and the remaining STAFF financial-scope corrections are not complete.
